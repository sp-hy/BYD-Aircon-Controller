## [2.15.0] - 2026-Aug-21

> **What's new in a nutshell**
>
> A big release for **plug-in hybrids** and for **knowing what your driving really costs**. On **DM-i / PHEV** cars, every trip's detail now carries a **Hybrid Breakdown** — how far you went on electrons vs petrol, litres burned, and consumption both ways — the **mode timeline** gains an **energy-mode lane** (EV / Force EV / HEV / Fuel / Keep) so you can see exactly where the engine kicked in, and the dashboard shows a **Fuel** and **combined** range next to the EV range. The **EV range projection** is honest now too: kilometres the engine drove no longer water down the electric consumption rate, so genuinely efficient EV driving can finally project past the rated figure. On the **cost** side, you can record **what each charge actually cost** — a price per kWh, or just the total you paid (**0** for a free charge) — and trips are then costed from the energy they truly used, **oldest energy first**, so a cheap home charge is spent before a pricier public one and a trip spanning both is billed at the exact mix; every completed charge also shows the **distance you drove since the last one**, and the **Monthly Cost** card gains a **cost-history chart**. **DiLink-5** owners get a solid reliability improvement: with **auto-start** enabled, trip recording now **resumes on its own in the background after a car off/on** — usually inside a minute — instead of working once or twice and then needing the app reopened by hand. The head unit force-stops apps when the car shuts off, and the app can now wake itself back up past that. One case is still open, and we're on it: these head units restart themselves overnight, and the app can't yet survive that restart unaided, so the first drive of the day may still need one tap to get going (DiLink-3 is unaffected throughout). Four **new cars** join the picker: the **Atto 3 SR (49.92 kWh)** and **Atto 3 Premium (74.88 kWh)**, the **Sealion 7 Design AWD (82.5 kWh)**, and — for Brazil — the **Song Pro DM-i** plug-in hybrid in both **GL (13.1 kWh)** and **GS (18.3 kWh)** trims. Finally, **dashboard animations now start off** are becoming a legacy feature — turn them back on any time in Preferences. Oh and the BYD Seal 6 95kW is now correctly set as RWD, and the **tap-the-logo screenshot** now works on the HV / 12V battery-history popup too.

- **Hybrid Breakdown card on a trip's detail view (plug-in hybrids)** — PHEV trips now show how the journey split between electrons and petrol: **electric share** (with a green/orange split bar), electric vs engine **distance**, **fuel used** in litres, **fuel consumption** both over engine-driven kilometres and spread across the whole trip, and **electric consumption over EV kilometres only**. Computed from the car's own lifetime EV/engine-mileage and fuel counters (summed as clamped per-point deltas, so a mid-trip counter reset can't corrupt the totals; trips too short for the whole-km engine counter fall back to odometer distance over engine-mode stretches). The card appears on trips recorded from this version on — the underlying fuel counters are now saved with every data point.
- **Energy-mode lane on the Drive/Regen mode timeline (plug-in hybrids)** — the trip Mode Timeline gains a third lane showing when the powertrain ran **EV / Force EV / HEV / Fuel / Keep**, colour-coded like the drive- and regen-mode lanes, so you can see exactly where in the trip the engine kicked in. Appears automatically on trips that have energy-mode data (including past trips — the mode was already being stored); BEVs never see it.
- **Fuel and combined range on the dashboard (plug-in hybrids)** — the Classic dashboard's power row gains a **Fuel** range metric next to the EV Range, and the Cards layout's Range tile subtitle now reads *"Fuel X · Total Y"* — the EV projection stays the headline number. Uses the car's own cluster fuel-range reading, with a fallback estimate from fuel % × tank size ÷ the car's average fuel consumption; tank capacities are now part of the car catalog (60 L for the Seal U DM-i / Sealion 6 DM-i / Song Plus DM-i families).
- **PHEV mock drive (dev)** — with a plug-in hybrid selected as the car, the hidden mock-drive button now simulates a DM-i style journey: starts electric, runs a charge-sustaining petrol stretch mid-cruise (fuel burning, engine warming toward 90 °C, SoC holding roughly flat), and returns to electric — with every fuel/ICE telemetry field populated. Lets PHEV features be developed and demonstrated without access to a real PHEV.
- **Trip-replay dev tool** — a developer-only companion to the mock button that replays an exported trip JSON (from "Save as JSON") through the app's *real* telemetry pipeline — trip detection, recording and the live range projection all run as if the car were producing the data, so a recorded drive shared by any owner becomes a permanent test fixture. Hidden behind the same compile-time flag as the mock button; the replayed trip is genuinely recorded into the database of the device it runs on.
- **Per-charge pricing & precise trip costs** — you can now record what each charge actually cost: open a charging session and set its **price per kWh**, or just enter the **total you paid** and the app derives the rate (enter **0** for a free charge). Trip costs are then worked out from the energy each trip really drew, **oldest energy first** — a cheap home charge is spent before a pricier public one, and a trip that spans more than one charge is billed at the exact mix. A charge with no price set falls back to your **global electricity tariff**, so nothing changes until you start entering real prices. The energy ledger is **reconciled against the battery's state of charge at every plug-in** — energy the pack lost outside recorded trips (pre-heating, idle drain) is written off oldest-first, so the price you set on a charge reaches the very next trips instead of being shadowed by stale bookkeeping. A trip's **Energy rate** row is read-only by design: prices are edited in exactly one place — the charging sessions and the global tariff — and flow through to every trip, so the two histories can never disagree. This makes cost tracking accurate for anyone who charges at more than one price (home vs public, paid vs free).
- **Distance since last charge** — each completed charging session now shows how far you drove since the previous charge — a chip in **Charging History** and a row in the session detail — anchored on the odometer at plug-in, for a quick read on real-world range between charges. Older sessions are filled in retroactively by deriving the odometer from your trips.
- **New cars in the picker** — **Atto 3 SR (49.92 kWh)** and **Atto 3 Premium (74.88 kWh)** join the existing 60 kWh car under **BYD Atto 3** (all FWD, 150 kW; note the Premium ships with **DiLink 5**, so it needs the dilink5 build). **Sealion 7 Design AWD (82.5 kWh)** joins **BYD Sealion 7**, the dual-motor counterpart to the 82.5 kWh RWD Standard with the same 160 + 230 kW split as the Performance trim. A new **BYD Song Pro DM-i** group covers the Brazilian 2026 plug-in hybrid in both trims — **GL (13.1 kWh)** and **GS (18.3 kWh)**, FWD, DiLink 3 — sold elsewhere as the Sealion 5 DM-i, but listed separately because the existing Seal 5 / Sealion 5 entries are the earlier 15.0 / 21.5 kWh generation.
- **Live PHEV energy mode on the dashboard** — the **Driving Dynamics** card now leads with the powertrain source the car is actually using (**EV / Force EV / HEV / Fuel / Keep**) before the regen and drive modes, so it's visible while driving rather than only afterwards in the trip's Mode Timeline. Also published as an **Energy Mode** sensor over MQTT for Home Assistant. Plug-in hybrids only — the card is unchanged on BEVs.
- **Diagnostics log can be saved or emailed, not just sent to Telegram** — **Settings → App diagnostics** now offers **Save locally**, which writes `diag.log` and the background-restarter snapshot into `Downloads/BydTripStats/` so they can be copied off with a USB stick and needs no network at all, and **Email via QR code**, which uploads the recent tail and shows a QR you scan with your phone to send the link on. Both sit alongside the existing Telegram button. A head unit with no working DNS previously had no way to get the file out — which is exactly the moment it's most wanted.
- **Much deeper `diag.log` detail around background start-up** — every app start now records **how long since the head unit booted**, which distinguishes a genuine cold boot from BYD's ignition-on re-broadcast of `BOOT_COMPLETED`; whether the start was a **UI launch** and what launched it; a once-per-boot line stating whether the app **came up with the head unit or missed that boot**, with a running hit-rate over the last 20 boots; and the state of the **background restarter** on every attempt — alive, re-dispatched, or unreachable, together with the local adb port and socket state and the Wi-Fi status. The restarter's own log is snapshotted before it can be overwritten and is attached to whichever export route you use. None of this changes behaviour; it exists so an "auto-start didn't work" report can be diagnosed from one file instead of guesswork.
- **Delete a Telegram backup from the app** — every entry under **Restore from Telegram** now has a **delete** button next to Restore, so old backups can be cleared without leaving the app. The entry is dropped from the list and the document message is deleted from your chat as well — but Telegram only lets a bot delete its own messages for **48 hours**, so for anything older the app says so plainly and leaves you to remove it in Telegram yourself.

### Changed

- **Trip & monthly costs now follow the price of each charge**, not one flat tariff — free and paid charges are reflected accurately via the per-charge pricing above. The **Monthly Cost** card gained an embedded **cost-history bar chart** (up to 13 months, with total and monthly average, so the current month lines up with the same month last year); the separate standalone "Cost history" card and the old manual **"en-route DC charging cost"** field on a trip were removed — the charge-price flow supersedes both.
- **Dashboard animations now default to off** for a snappier head-unit UI on new installs — enable them any time in **Settings → Preferences**. If you'd already turned them on, that choice is kept.
- **Temporary upload links now last 24 hours** instead of 12 — both the compatibility report and the new diagnostics-log upload, giving more slack between generating a link and someone actually downloading it.

- **PHEV range projection no longer dilutes the electric consumption rate with engine-driven kilometres** — distance covered while the engine propels the car (fuel visibly flowing, or the powertrain in HEV/Fuel mode) is now excluded from every consumption-rate input (rolling window, speed bins, trip average) while the battery energy is kept, so the measured Wh/km reflects *electric driving only*. On a series-hybrid stretch (engine generating while the pack still discharges) the rate can only err toward a *shorter* range — never a longer one. With the rate honest on its own, the old hard "never better than catalog" PHEV floor is **softened to 0.7 × catalog**, so genuinely efficient EV-mode driving can finally project above the rated figure (the floor remains as a safety net for trips recorded before the fuel counters were saved per point, and for firmware that never reports them). The same gating applies when a trip is rebuilt after reopening the app, using the newly persisted per-point fuel signals, so live and restored curves agree. BEVs are completely unaffected — every kilometre is an EV kilometre there, and the new accounting reduces exactly to the previous behaviour.

### Fixed

- **Only real backups show up in the Telegram restore list** — the same private bot chat is also used to send **diagnostics logs**, **compatibility probe reports** and **exported trips**, and each of those was being recorded as a restorable "backup", cluttering the list with files that could never be restored. Only `.db` database backups are listed now, and an already-cluttered list cleans itself up the next time you open Backup & Restore.
- **Wi-Fi status now reported reliably (MQTT / Home Assistant)** — the `wifi_ssid` value (and the Wi-Fi row in diagnostics) previously showed **blank whenever location was off** — which on a head unit is effectively always — so "Wi-Fi is up" was indistinguishable from "Wi-Fi is down". It now reflects the actual **Wi-Fi connection state** (via connectivity info, which needs no location permission): the network name when it's readable, **"connected"** when Wi-Fi is up but the name isn't, and blank **only** when Wi-Fi is genuinely down. A non-blank value now reliably means the car's Wi-Fi link is up — useful for telling, while parked, whether the head unit is still on Wi-Fi or has fallen back to mobile data.
- **BYD Seal 6 95kW** — it was incorrectly set as FWD, it is now RWD as it should be.
- **DiLink-5: automatic trip recording resumes after car off/on** — on DiLink-5 the head unit force-stops apps when the car is switched off, and the app's background wake-up couldn't reach a force-stopped app — so with **auto-start** enabled, recording would resume for the first cycle or two and then stop until you reopened the app by hand. The background wake now reaches the app even after a force-stop, typically bringing recording back within a minute, so trips keep recording across ignition cycles without touching anything. **One case is still under investigation**: these head units restart themselves overnight, and the background helper does not survive that restart, so the first drive after an overnight restart may still need the app opened once by hand. DiLink-3 is unaffected throughout.
- **The background helper is re-checked repeatedly instead of only at start-up (DiLink-5)** — it used to be set up only when the app process started and on vehicle broadcasts, so if the local channel it depends on wasn't ready at that exact moment — typically about 20 seconds into a cold start — the car could go the rest of the day with no helper and no second attempt. It is now retried 30 seconds, 90 seconds and 5 minutes after start-up, and again on every 15-minute service watchdog tick, stopping as soon as it succeeds. The fast service catch-up kicks (15 s / 45 s / 2 min) also now run on **every** start path rather than only after a vehicle broadcast, so a process killed during the start-up rush recovers in seconds instead of waiting up to 15 minutes.
- **Screenshot now works on the HV / 12V battery history view** — tapping the BYD logo to save a screenshot (Pro) did nothing on the **HV / 12V Batteries – Last 48 Hours** popup. That view is a dialog with its own window, so the capture was grabbing the dashboard behind it instead of the popup; it now captures the dialog itself, and the 48-hour battery charts save correctly like every other screen.

## [2.14.0] - 2026-Jul-29

> **What's new in a nutshell**
>
> A **DiLink-5 follow-up** release, again with big thanks to **[@cagdasbas](https://github.com/cagdasbas)**. On the **BYD Sealion 7**, the **regen (energy-recovery) level now reads the car's real setting** — **Standard / High** — live from the vehicle instead of a placeholder; the dashboard **no longer shows a bogus road-slope figure** (a false **−1.0°** the Sealion 7 doesn't actually report); and a **backup-list bug is fixed** so saved backups show their real **size and date** instead of appearing as empty **0-byte / 1970** files, with the newest copy offered when the same backup exists twice. DiLink-5 also gets a **clearer update flow**: since a head unit can't complete an in-app install, the app now shows that a new version is available together with the **exact `adb install -r` command** to run from a PC or phone (**Settings → About & FAQ**), instead of a Download button that can't finish — the update **badge still appears** as before. On **DiLink-3**, this release **restores instant live speed and power** for setups using the fast telemetry path, which had dropped to a once-a-second update after a head-unit firmware change. As always the two builds are separate, and neither change affects the other vehicle.

### Added

- **DiLink-5: real regen (energy-recovery) mode** — the regen level shown on the dashboard now reflects the Sealion 7's actual **Standard / High** energy-recovery setting, read live from the car, instead of a fixed placeholder value. Contributed by **[@cagdasbas](https://github.com/cagdasbas)**.
- **Fuller compatibility reports on DiLink-5** — the vehicle-compatibility report (the diagnostics export you can send in to help add or verify vehicle support) now also captures the live values the car **pushes** through its typed listeners on DiLink-5 — state of charge, range, mileage, speed, charging power, motor RPM / voltage / current, regen level, battery and tyre temperatures, air-quality (PM2.5), road slope and more. On DiLink-5 most of these arrive as pushed events rather than readable getters, so the earlier getter-only sweep couldn't see them; capturing them makes Sealion 7 reports far more complete (and the report's data collection is now thread-safe). Contributed by **[@cagdasbas](https://github.com/cagdasbas)**.
- **Custom currency for the electricity tariff** — the currency picker (Settings → Preferences → Electricity tariff) now has a **Custom…** option where you can type your own symbol — **kr, zł, ₺, Fr** and the like — instead of waiting for it to be added to the built-in list. Your symbol is saved and used everywhere costs are shown (trip history, monthly totals, trip details). The built-in shortcuts (€, £, $, A$, ฿, R$, RM) are unchanged.

### Changed

- **DiLink-5: app updates are now installed manually** — a DiLink-5 head unit can't complete the in-app silent install (and a self-relaunch there risks a boot loop), so instead of a **Download** button the **About & FAQ** screen now shows that a new version is available (with its version number and release notes) and the **exact command to install it** — `adb install -r <apk>` — from your PC or phone, plus a link to the releases page. The **update badge still appears** as before. **DiLink-3 auto-update is unchanged** — it still downloads and installs in-app.

### Fixed

- **DiLink-5: no more false road-slope reading** — the dashboard no longer shows a bogus **−1.0°** road slope on the Sealion 7 (which doesn't report one); it's simply omitted where the car doesn't provide it. Contributed by **[@cagdasbas](https://github.com/cagdasbas)**.
- **DiLink-5: backups list correctly** — saved backups no longer appear as **0-byte / 1970-dated** entries. Each now shows its real **size and modification date**, and when the same backup exists twice the **newer/larger copy** is the one offered to restore. Contributed by **[@cagdasbas](https://github.com/cagdasbas)**.
- **DiLink-3: instant live speed & power restored** — on setups using the fast (privileged) telemetry path, live **speed and power** had dropped from instant to about once a second after a head-unit firmware change tightened a security check; the fast path works again. Vehicles without that path are unaffected — they already update roughly once a second.

---

## [2.13.0] - 2026-Jul-22

> **What's new in a nutshell**
>
> The headline is **DiLink 5 support** — BYD Trip Stats now runs on **DiLink-5 head units**, bringing the full app to the **BYD Sealion 7** with live telemetry across the board — state of charge, range, odometer, charging power, speed, per-wheel tyre pressure, battery health and more. It ships as a **dedicated build alongside the existing DiLink-3 one**, so **DiLink-3 vehicles are completely unaffected** — and huge thanks to **[@cagdasbas](https://github.com/cagdasbas)**, who contributed the entire DiLink-5 port. Beyond that, the electricity-tariff currency picker gains **Malaysian Ringgit (RM)** and now **pre-selects a sensible available currency from your head unit's locale** on a fresh install instead of always defaulting to €. Backups gain a new **"Restore from file…"** action to restore any `.db` the automatic list can't surface — handy if a backup lives in another folder or came over from another device. On the dashboard, the **battery tile is now tappable** — tap it to switch the reading from **state of charge** to the **remaining battery energy in kWh**, then tap again to switch back. And if the **wrong build** ever ends up on a car, the app now **warns you** to install the right one rather than quietly misbehaving.

### Added

- **DiLink 5 support (BYD Sealion 7)** — the app now runs on DiLink-5 head units via a dedicated build, reading live telemetry — state of charge, range, odometer, charging power, speed, per-wheel tyre pressure, battery health and more — on the Sealion 7. Includes the three battery variants (71.8 / 82.5 / 91.5 kWh). DiLink-3 vehicles are unaffected. DiLink-5 support was contributed by [@cagdasbas](https://github.com/cagdasbas) — huge thanks!
- **Tap the SoC tile to see remaining kWh** — the live battery tile on the dashboard (both Classic and Cards layouts) is now tappable: tap once to switch the reading from **state of charge (%)** to the **remaining battery energy in kWh**, and tap again to switch back — a quick glance at how much is actually left in the pack. It's a display-only toggle; your **SoC source (Panel / BMS)** choice under Settings → Preferences stays exactly as set. Works on all vehicles.
- **Malaysian Ringgit (RM / MYR)** added to the electricity-tariff currency options (Settings → Preferences → Electricity tariff), alongside €, £, $, A$, ฿ and R$.
- **Currency now defaults to your device locale** — on a fresh install, the electricity-tariff currency is pre-selected from the head unit's system locale (e.g. RM for Malaysia, ฿ for Thailand, $ for the US) instead of always starting at €. Existing users with a saved currency are unaffected.
- **Restore a backup from a file** — Settings → Backup → *Restore from file…* opens a built-in file browser to pick any `.db` backup, so you can restore one the app's list can't show (a backup from another folder, another device, or one copied over via adb) — no third-party file manager needed.
- **Wrong-build warning** — if a DiLink-5 head unit is running a build without DiLink-5 support, the app now warns you to install the correct build (telemetry won't work otherwise).

### Hotfixes

Two post-release hotfixes were issued for 2.13.0, both **DiLink-5 only** (DiLink-3 was never affected) and both rebuilt under the same `2.13.0` version name:

- **2026-Jul-25 · revision 1 — head-unit boot loop from an app self-restart.** On DiLink-5, several in-app actions that **kill and auto-relaunch the app** — *Trim database* (its disk-reclaim / VACUUM step) and *Restore backup* / *Wipe data* / *Reset app* — could relaunch while the DiLink-5 SDK was still initialising, wedging the car's OEM data service and sending the **head unit into a boot loop** (warning lights, ADAS unavailable), recoverable only by a **full car power-off**. Resolved by no longer auto-relaunching on DiLink-5: *Trim* skips the VACUUM + restart step there, and the other actions simply close the app (you reopen it yourself). DiLink-3 keeps the original behaviour.
- **2026-Jul-25 · revision 2 — head-unit boot loop from the compatibility probe.** With the compatibility probe enabled on DiLink-5, one diagnostic step **invoked every no-argument method** the car's devices expose — which on DiLink-5 includes destructive ones (e.g. reset / clear-all) — again wedging the OEM service into a **boot loop**. Resolved by disabling that indiscriminate method sweep on DiLink-5; the curated, safe probe reads are kept.

---

## [2.12.0] - 2026-Jul-11

> **What's new in a nutshell**
>
> A big customisation-and-looks release, built around a brand-new **Cards dashboard layout (Pro)** — an alternative to the classic chart-centric dashboard that trades the large central graph for **big, well-spaced tiles with much larger fonts**. The top **power row** (Power / Speed / SoC / Range / Distance) and the bottom **Trip Tracking** bar stay put; the **two middle rows are fully yours** — tap the **pencil** in the top-right to enter edit mode, where every tile gently **wobbles** and you can **drag to reorder** them and **show/hide** the ones you don't care about, with the rest **growing to fill the freed space**. The range-projection **chart sits pinned in the centre**, spanning both rows, and **expands to a full-size window on tap** (or from the Range tile); hide it and the cards reclaim the whole width. Even the five **power tiles are reorderable** (they're always visible), and they gained **live subtitles** — Power reads *Idle / Accelerating / Decelerating / Charging*, Speed reads *Parked / Driving*. It also **adapts to split-screen**, reflowing to a single compact screen when the app runs side-by-side with another. There's also a new **Neon theme (Pro, dark only)** — a pure-black, OLED-friendly look whose **power digits and accent stripes glow like neon** and whose cards carry a soft accent-coloured edge. And across the board the dashboard is now **subtly 3-D**: cards, the top bar and the trip bar all cast a soft shadow so they lift off the background. The classic layout and every other theme are untouched — these are opt-in choices under **Settings → Preferences**.

### Added

- **Cards dashboard layout (Pro)** — a second dashboard style, chosen under **Settings → Preferences → Dashboard Layout** (Classic stays the default and is available to everyone). It drops the big central chart in favour of **large, separated tiles with noticeably bigger, more glanceable numbers**. The **power row** at the top and the **Trip Tracking** bar at the bottom are fixed; the **two middle rows** are a customisable grid:
    - **Edit mode** — a **pencil** button in the top-right (it turns into a green check while editing, and the other top-bar icons step aside so it's clear you're customising). In edit mode the tiles **wobble** to signal they're rearrangeable.
    - **Drag-to-reorder** — drag any tile to a new position; the others slide out of the way and the layout is saved instantly. The **five power tiles are reorderable too** — but they're **always visible** (never hideable).
    - **Show / hide** — each middle card has a corner badge to hide it (it dims, then disappears on exit) or bring it back; hidden cards free up space and the **remaining cards resize to fill it**.
    - **Centre range-projection chart** — the projection chart is **pinned in the middle**, spanning both rows, flanked by two cards on each side. **Tap it (or the Range power tile) to open a large full-screen view.** It's modular like the cards: hide it and the eight cards reflow to full width, after which it's reachable only from the Range tile.
    - **Live power-tile subtitles** — **Power** shows *Idle · Accelerating · Decelerating · Charging* depending on what the car's doing, **Speed** shows *Parked · Driving*, and Range/Distance/SoC carry context labels.
    - **Adapts to split-screen** — when the app is shown in a narrow multi-window (head-unit split view) the whole dashboard **reflows to fit one screen with no scrolling**: the power metrics condense to a single value-only row, the cards become compact single-line rows (title on the left, value on the right), and the range chart takes all the remaining height with its labels stripped so the plot itself is bigger. Tapping the Range tile still opens the full chart, and full-screen is completely unchanged. Editing is a full-screen action, so the pencil is hidden while split.
- **Neon theme (Pro, dark only)** — a fourth theme option alongside System / Light / Dark. It paints the whole app **pure OLED black**, and in the dashboard the **power-metric digits and their left accent stripes glow** in their accent colour, while every card gets a subtle **accent-tinted edge** so it reads as a lit surface against the black. It works in both dashboard layouts. (The glow is rendered so it looks right on any head-unit Android version, not just the newest.)
- **"Wi-Fi only" option for Telegram auto-backups** — a new toggle under **Backup &amp; Restore → Telegram** that restricts scheduled backups to **Wi-Fi (unmetered) connections**, so the weekly/daily upload of your database (up to 50 MB) won't quietly eat a limited mobile-data plan. It's **on by default**; turn it off to let auto-backups run on any connection. Manual backups are unaffected — tapping **Send** always uploads immediately, whatever the network.
- **Atto 1 (Dynamic / Premium) added to the car picker** — the two China-spec Seagull variants now appear under **BYD Seagull / Dolphin Surf / Atto 1** with their correct **30.08 kWh (Dynamic)** and **38.88 kWh (Premium)** packs, 45 kW motor and WLTP ranges (230 / 300 km). These are a different battery family to the European Dolphin Surf, which stays as its own Active / Boost / Comfort variants in the same group.

### Changed

- **The whole dashboard is now gently 3-D** — dashboard cards, the top app bar and the Trip Tracking bar all cast a soft drop shadow, so they lift off the background instead of sitting flat. Applies in both the Classic and Cards layouts and in every theme.
- **Pro-only options are now shown but locked, instead of hidden** — the **Cards** layout button (Dashboard Layout) and the **Neon** theme button now always appear in Settings, shown **dimmed with a padlock** when you're not on Pro, so you can see what Pro unlocks rather than not knowing it exists. They select normally once Pro is unlocked.
- **Tidier top bars in split-screen, and a consistent back button on the HV/12V history** — Trip History, Charging History and the **HV/12V — Last 48 hours** view now hide their small subtitle hint when the window is too narrow to show it (it's kept in fullscreen). And the HV/12V history view's close control is now a **back arrow** like every other screen, instead of an **✕**.

### Fixed

- **Corrected Atto 1 tyre pressures** — the Atto 1 (Dynamic / Premium) now uses the door-sticker **2.7 bar front and rear** instead of the previous 2.5 / 2.5, matching the placard on the driver's door.

---

## [2.11.0] - 2026-Jul-05

> **What's new in a nutshell**
>
> The charging detail view gains a **Cell V** tab showing each cell's high and low voltage across a session — so you can watch the pack's voltage climb as it fills and see how tightly the cells stay balanced (the high−low spread). And every charging chart — Power, Voltage, Cell V and Temperature — can now be plotted against **time or state of charge** with a Time / SoC toggle.
>
> On the driving side, the **live range-projection curve** is far steadier and better-bounded. The wild up-and-down on hilly motorways — a long descent sending the projection shooting up, the next climb dropping it — is reined in by an **optimism cap** (it can no longer claim you're driving dramatically better than your own trip average) plus source-level smoothing, and the violent spikes in the **first kilometre** of a drive are gone (the estimate now starts from your lifetime average and eases onto the trip's own rate). It also no longer collapses to zero on a stray reading, and reopening an in-progress **plug-in-hybrid** trip no longer overstates its EV range. It's still a *live* estimate — it tracks how you're driving, so it moves with the road — but it should feel much closer to a trip computer now, without the fantasy swings. And a trip's **mode insights** now always compare the drive (and regen) modes you actually used, letting the numbers decide which was more efficient rather than assuming Eco always beats Sport.
>
> The whole app is now **multilingual** — every screen is fully translated into **27 languages** (28 including English), pick yours under Settings → Language, and it overrides the head unit's own language for the app only.

### Added

- **Multilingual support — the app is fully translated into 27 languages** — every screen, dialog, chart label and FAQ entry now has a complete translation; there's no hardcoded English left in the UI. Choose your language under **Settings → App → Language** (it overrides the car's system language for BYD Trip Stats only, and *System default* follows the head unit). Bundled languages: Greek, German, French, Spanish, Italian, Dutch, Polish, Czech, Hungarian, Romanian, Swedish, Finnish, Danish, Norwegian, Portuguese, Brazilian Portuguese, Russian, Turkish, Thai, Hindi, Hebrew, Indonesian, Vietnamese, Japanese, Korean, and Simplified &amp; Traditional Chinese. Product terms (mode names like Eco / Normal / Sport, ABRP, MQTT, Pro) are kept in their original form across every language. Hebrew is right-to-left; the layout mirrors accordingly.
- **"Cell V" tab in Charging Detail** — a new tab plotting the **highest and lowest cell voltage** across a charging session, with the high−low **spread** (cell imbalance) shown in mV on the crosshair. Like the Temperature tab, each line starts where the BMS actually begins reporting the per-cell range — some firmwares only expose it partway through a charge — and the axis scales to the real ~3 V window. Handy for spotting a weak cell or watching the pack top-balance near full. Built on cell-voltage data the app already records, so it works for past sessions too.
- **Time / SoC x-axis on the charging charts** — the **Voltage**, **Cell V** and **Temperature** charts gain the same **Time | SoC** toggle the Power chart already had, so any of them can be plotted against elapsed time or against state of charge, and the choice is **shared across all four charts** (switch one to SoC and they all follow) — handy for lining a voltage or temperature curve up to a given SoC. In time mode each chart now also overlays the **SoC** curve on a right axis (as the Power chart did), so SoC context is always visible. The old **"Power + SoC"** tab is renamed **"Power"** now that SoC is selectable everywhere.

### Changed

- **Consistent branded top bar on every screen, and screenshot-anywhere** — every screen now shows the BYD logo + "trip stats" wordmark at the far left of its top bar (previously only the dashboard had it), sized to match the dashboard exactly. The back/close button now sits **after** the logo rather than sharing its corner, so a mistimed tap can no longer land on the logo instead of Back. Tapping the logo saves a screenshot (Pro) from **any** screen now, not just the dashboard.
- **Trip History / Charging History subtitle moved below the title** — the small explanatory hint under "Trip History" / "Charging History" now sits on its own line beneath the title, matching Seasonal Analysis, Tags and Routes, instead of being squeezed beside it.
- **Internal: large UI screens split into focused per-tab / per-component modules** — charging detail, route analysis, heatmaps, dashboard, settings, trip detail / compare / history and battery degradation were each broken out of their monolithic files into smaller modules, and all four charging charts (Power, Voltage, Cell V, Temperature) were consolidated onto one shared chart engine (which is what let the Time / SoC toggle and SoC overlay be single-place changes). No visible behaviour change; purely maintainability.
- **Drive- and regen-mode insights now always compare, and never assume which mode "should" win** — the Mode Insights card on a trip's route analysis previously only spoke up for a few hardcoded cases (Eco vs Normal being equal, Sport costing more than Normal, High regen not helping), and stayed **silent** the rest of the time — including when you drove **Eco and Sport with no Normal in between** (no comparison appeared at all), or whenever the outcome ran against the textbook expectation. That last part mattered: you can drive Eco with your foot down and Sport gently, so Eco isn't *always* the more efficient run. The insight is now **symmetric and driven purely by what was measured** — whenever a trip used **two different drive (or regen) modes** each over a meaningful distance, it always reports a result, naming whichever mode the data shows was actually more efficient (or better at recovering energy), by how much, or that the two were about the same. No mode is assumed better than another; the numbers decide. When a trip used all three drive modes, it compares the two you drove the most.
- **Range projection: a smarter cold start, and an honest model badge** — in the first moments of a drive, before the trip itself has gathered enough data to estimate consumption, the projection now uses **your own lifetime average consumption** across all past trips (once you've logged at least ~50 km of history) instead of the car's fixed catalog rate — a far more realistic starting point that reflects how *you* actually drive. As the trip builds up its own data the projection moves on to this-trip and recent-driving estimates exactly as before. The projection-model badge that used to read **"● Speed bins"** is now labelled **"● Trip average"**, which is what that tier actually shows — this trip's running average consumption — so the badge no longer implies a per-speed-band model it isn't using.

### Fixed

- **Charging chart y-axis labels collapsed to whole numbers on a flat session** — e.g. a battery-voltage or temperature chart that barely moves gave an axis reading just two or three repeated whole numbers. Tick labels now use enough decimal places for the zoom level — "3.90, 3.92, …" — across all the charging charts.
- **Charge power now reports the accurate, decimal figure during AC charging** — the charge power on the dashboard, in the recorded session curve, and on the MQTT `charging_power` sensor is now taken from the instrument cluster's own charge-power reading — the same value the BYD app shows (e.g. 6.1 kW) — on cars that expose it. On cars whose dedicated charging sensor under-reports (an Atto 3 showed a flat ~2 kW for a real ~4 kW charge, wall socket confirmed 4.6 kW), or for a DC charge begun with the car switched on where that sensor can stay silent, it falls back to the battery pack's own power reading. Energy totals are unchanged (those come from the State-of-Charge gain); pure-EV only.
- **Projected-range curve swung wildly on hilly routes, spiked in the first kilometre, and could reach physically-impossible figures downhill** — several causes, fixed together so the line is much steadier:
    - **Downhill balloons are now bounded.** A long descent drives your *measured* consumption toward zero, which ballooned the projected range (reported shooting past 500 km before snapping back on the next climb). An **optimism cap** now bounds it — the projection can't claim you're driving more than ~1.5× better than your own demonstrated trip average. It's one-sided on purpose: a climb reading *worse* than your average still shows its shorter range honestly, so range is never over-stated toward stranding you.
    - **The rate no longer jitters.** Consumption was read from just the two ends of the rolling window, so the battery meter's coarse steps made it wobble; it's now a **least-squares fit across the whole window**, and the window's energy is built from clamped per-sample steps so a mid-trip meter re-baseline or a burst of regen can't inject a false spike.
    - **The start of a drive is calm.** Dividing energy by a near-zero distance is numerically wild, so the first few hundred metres used to spike between extremes. The estimate now **starts from your lifetime average and eases onto the trip's own rate** over the first 3 km.
    - **No more step at 3 km.** The hand-off from the "trip average" model to the "recent driving" model was an instant jump; it's now a smooth crossfade.

    The projection is still a live estimate that tracks how you actually drive — but without the fantasy swings. The same fixes apply to a reopened trip's rebuilt curve, so it still matches the live one.
- **Last distance label on the range-projection chart was clipped** — the right-most x-axis value (e.g. "12.6") had its trailing digit cut off at the chart's edge because it was centre-anchored on the boundary. The first and last labels are now edge-anchored so they stay fully on-screen.
- **Projected range could briefly collapse to zero (and stay scarred)** — a single stray telemetry packet where state-of-charge momentarily read 0 made the projection compute a 0 km range, drop the curve to the floor and back, and — worse — bake that spike into the saved curve so it survived every reopen of the trip. Such a reading is now treated as "no data for this instant": the line simply leaves a gap instead of diving to zero.
- **Reopening an in-progress plug-in-hybrid trip could overstate its EV range** — when a PHEV trip was rebuilt from storage (reopening the app mid-drive, or after the background service restarted), it estimated remaining EV energy from battery-capacity × state-of-charge, while the **live** view used the BMS's own remaining-EV-energy reading — which correctly sets aside the charge the car reserves for hybrid operation. So a reopened curve could project more EV km than the live one near the bottom of the charge. The app now records that BMS reading with every data point (a one-time database upgrade) and the rebuilt curve uses it, so reopening a trip matches the live projection. Pure-EV cars are unaffected (they never hit this path), and trips recorded before the upgrade fall back to the old estimate exactly as before.

---

## [2.10.0] - 2026-Jun-27

> **What's new in a nutshell**
>
> A trip-organisation release built around three features that work together. **Trip merging** stitches two trips that were really one journey — split by a brief stop like a petrol station or a red-light timeout — back into a single trip, with honest numbers (distance, energy and driving time are added up from each trip's own figures, so a quirk where the battery's discharge counter resets between trips can't distort the result). **Recurring route detection** automatically spots journeys you drive again and again — your daily commute, say — and gives them a new **Routes** screen where you can compare how efficient each run was. And **trip tagging** lets you label trips with reusable, colour-coded tags like "commute", "motorway" or "errand", filter your history by them, and see a per-tag breakdown of distance and efficiency on a new **Tags** screen. It also fixes a gap where a **DC fast charge started with the car switched on** could go unrecorded — the app now reads the car's own charge-power meter to catch it — and tidies up the charging **Temperature** chart so its axis scales to the real range instead of being squashed by not-yet-reported cell readings. For people running their own MQTT broker, the publisher now also speaks **MQTT over WebSocket (ws/wss)** with an explicit TLS toggle, so the broker can sit behind an HTTP reverse proxy instead of needing a raw MQTT port opened, and it now also publishes the **connected WiFi network name** (the `wifi_ssid` sensor), which had previously always come through blank in Home Assistant. And on the diagnostics side, sharing a **vehicle-compatibility probe** is now one tap — the report uploads to a temporary host and the app shows a **QR code** that opens a pre-filled email with the download link, so you can send it from your phone without Telegram or a file transfer.

### Added

- **Trip merging** — combine two trips that were really the same journey, separated by a brief stop (petrol station, charging stop, a red-light timeout that ended the trip early). Select two trips in History's selection mode and tap **Merge**: the earlier trip survives and absorbs the later one, taking over its data points, route segments and tags. Only contiguous trips can be merged — they must be close together in time and continuous on the odometer — so unrelated trips can't be combined by accident. Crucially the merged trip carries **real** numbers, not timeline-derived ones: distance, energy and driving time are the **sum of each trip's own recorded values** (so the BYD discharge counter resetting between the two trips can't corrupt the combined energy, and parked time in the gap isn't counted as driving), while the **start** of the earlier trip and the **end** of the later trip are kept as the journey's real endpoints. This action can't be undone.
- **Recurring route detection** — the app now automatically groups completed trips that are really the same journey so you can compare their efficiency over time. A new **Routes** screen (toolbar icon in History, next to Seasonal) lists each route you've driven **3 or more times**, showing its average, best and worst efficiency, average duration and a per-trip efficiency trend chart; tap any instance to open that trip. Two trips count as the same route when their **start and end locations match** (within ~250 m) **and** their distances are similar — and direction matters, so your commute **to** work and the drive **home** are tracked as separate routes (their efficiency genuinely differs with elevation, traffic and time of day). All computed on-device from coordinates the app already records; no new data is collected.
- **Trip tagging** — label trips with reusable, **auto-coloured** custom tags (e.g. "commute", "motorway", "errand"). Add or remove tags on a trip's **detail screen**, or **bulk-tag** several trips at once from History's selection mode (alongside Merge / Compare / Delete); tags appear as coloured chips in the History list. **Filter** the history list by one or more tags from the filter sheet, and a dedicated **Tags** screen rolls up each tag's trip count, total distance, total energy and average efficiency — so you can compare categories like "motorway" vs "city" at a glance — with options to rename or delete a tag, or jump straight to its trips. Tags are created once and reused; deleting a tag keeps the trips, and merging two trips combines their tags.
- **Send the vehicle-compatibility probe report by QR code** — sharing a compatibility report no longer needs Telegram, adb, or fishing a saved file off the head unit. Settings → *Vehicle Compatibility* gains an **Email via QR code** button: it uploads the report to a temporary public host (litterbox — auto-deleted after **12 hours**) and shows a **QR code** that opens a pre-filled email to bydtripstats@gmail.com with the one-tap download link already in it. Scan it with your phone, pick your email account and press Send — the head unit has no mail app, so this hands the report off to the device that does. A **Copy link** fallback and the raw URL are shown too, and the privacy notice now spells out that this option uploads the report to a temporary public host. The existing **Telegram** and **Save** (to Download) options are unchanged.
- **MQTT over WebSocket (ws / wss), plus an explicit TLS toggle** — the MQTT publisher previously connected only over raw TCP, which can't be routed through an HTTP reverse proxy. You can now enable **WebSocket** transport (with a configurable path, default `/mqtt`) and **TLS** independently in Settings → Connections → MQTT, so users running their own broker on a home server can expose it through nginx / Traefik / Caddy as `wss://your-host/mqtt` instead of opening a raw MQTT port. The broker must have a WebSocket listener enabled (e.g. Mosquitto's `protocol websockets`); the reverse proxy forwards to it. Plain-TCP setups are unaffected — both toggles default off, and existing TLS-on-8883 configs keep working.

### Fixed

- **A DC fast charge started while the car was switched on wasn't recorded as a charging session** — charging detection leaned on the car's *pushed* BMS charging signals (charge-gun / charger-work state, delivered-capacity), and on this firmware that channel can stall: a DC charge begun with the head unit fully powered left them silent, so the dashboard showed the live charge but no session opened. The app now reads the car's own **charge-power meter** (battery voltage × current) directly — a reading it previously logged but never used, and which the old power path couldn't see because it was capped around the 50 kW AC range — so the charge is both detected and recorded with its true power curve (instead of the 0 kW stored before). As a backstop it also treats a large, sustained power flowing *into* the pack while parked as charging. **AC charging is untouched**, and it's pure-EV only — so a plug-in hybrid charging its pack from the engine in Park is never mistaken for a plug-in charge. Charging while the car is **off** is unchanged — still reconstructed from the State-of-Charge gain on the next wake-up.
- **MQTT entities could get stuck "unavailable" in Home Assistant even while telemetry kept publishing** — the publisher announced itself `online` only once, at connect, and relied on the Last Will to mark it `offline` on an unexpected drop. If a stray `offline` ever landed while the app was actually still connected and publishing — for example a retained will arriving after the broker handed the client ID to a fresh connection — nothing ever flipped availability back to `online`, so Home Assistant showed the device unavailable even though fresh state was arriving on the same broker. The app now re-asserts the retained `online` availability periodically (at most once every 30 s, so it adds no meaningful traffic to the live stream), letting availability self-heal within one cycle. Seen with self-hosted brokers; brokers on a always-reachable host rarely produced the stray `offline` in the first place.
- **WiFi SSID was always empty (blank in Home Assistant and in Settings)** — the `wifi_ssid` field was wired through to MQTT and the Settings "Wi-Fi" row, but the value was never actually read — the telemetry builder hardcoded it to an empty string, so the MQTT payload omitted it entirely. The app now reads the connected network name from the head unit (cached, refreshed at most every 30 s) and publishes it. Note: Android only exposes the SSID when location permission is granted **and** location is switched on; with location off the system reports it as unknown, so the field stays blank by design.
- **Charging "Temperature" chart was squashed, with the Cell min / Cell max lines pinned to 0 °C early in a session** — some BYD battery firmwares report only the *average* cell temperature at first and don't expose the per-cell minimum/maximum until partway through a charge. The chart drew those not-yet-reported readings as 0 °C, which both anchored the two lines to the bottom and pulled the whole temperature axis down to 0 — squashing the real ≈30–40 °C range into a sliver at the top. Each line now starts where its data actually begins, and the axis scales to the temperatures present, so the variation is readable. The average line is unchanged — it was available throughout.

---

## [2.9.1] - 2026-Jun-20

> **What's new in a nutshell**
>
> A maintenance release focused on accuracy. Plug-in hybrid **range projections are no longer wildly inflated** — the live trip loop now reads the actual selected car instead of assuming a big EV battery — and the **Range** readout no longer briefly folds in the fuel range. The **battery degradation** chart, the "Current SoH" card and the battery-health report now match the dashboard's State of Health to one decimal: no more rounded readings, and no more impossible upward tick where the old and new data met (your existing history is upgraded automatically). And the **Brazilian Real (R$)** joins the electricity-tariff currencies.

### Added

- **Brazilian Real (R$ / BRL)** added to the electricity-tariff currency options (Settings → Preferences → Electricity tariff), alongside €, £, $, A$ and ฿.

### Fixed

- **PHEV projected range wildly too high (saturating at the WLTP cap, and a "Range" readout that jumped between values like 75, 446 and 823 km)** — the projection was being computed with a fallback battery size of ~82.56 kWh and as if the car were a pure EV, because the live trip loop wasn't reading the selected car correctly (it relied on a value that was only populated while a specific screen was observing it, and otherwise stayed empty). On a small-battery plug-in hybrid like the Sealion 6 DM-i that hugely inflated the projection — so it pinned at "≥ WLTP" and the Range figure swung around — while it happened to look right on ~82 kWh electric cars. The trip loop now always reads the real selected car (battery size, PHEV status and rated consumption), so the projection uses the correct usable EV capacity and consumption floor again. The **Range** readout under the chart also now prefers the app's stable projected EV range over the car's raw BMS electric-range field (which on PHEVs can briefly report a combined electric **+ fuel** range). BEVs are unaffected.
- **Battery Degradation showed a rounded SoH (e.g. 97.0%) while the dashboard showed the precise value (97.6%)** — the degradation chart and the battery-health report averaged the rounded *integer* SoH stored per data point, losing the decimal the dashboard reads from the car's precise statistic value. The precise SoH is now recorded in its own column and used for the chart, the "Current SoH" card and the exported report, so they match the dashboard to one decimal. Your existing history is upgraded automatically: a one-time background backfill recovers the precise value that was already saved inside each older data point, so the whole SoH series becomes decimal-accurate — and the line no longer ticks *up* (which SoH should never do) where the old rounded data met the new precise recording.

---

## [2.9.0] - 2026-Jun-19

> **What's new in a nutshell**
>
> Introduces **BYD Trip Stats Pro** — an optional one-time unlock for premium features, mostly as a 'thank-you' gesture: a **cell imbalance alert**, an exportable **battery health report** (PDF/HTML) for resale or warranty disputes, **dashboard screenshots**, and **backups to an SD card** (which survive a firmware downgrade or factory reset). Pro is unlocked with a one-time per-vehicle code, verified entirely on-device — nothing leaves your car. Everything that was free continues to stay as free. Elsewhere: the background service now defaults to **Always On** so parked telemetry and overnight-charging detection keep working, **Settings → Preferences** is reorganised into clear groups, and the battery degradation chart now **automatically hides the old, under-reported SoH readings** from before v2.4.0 so the trend is accurate. Also fixes the Energy Consumption chart flatlining on longer trips, the Speed/Power charts smoothing away brief peaks, and inflated PHEV range projections.

### Added

- **BYD Trip Stats Pro** (Settings → Preferences → *BYD Trip Stats Pro*) — an optional unlock for premium features via a short, one-time unlock code tied to your vehicle. **Scan the on-screen QR code with your phone to email it in one tap** (the QR opens a message to bydtripstats@gmail.com with your Vehicle ID (hashed/encrypted VIN) already filled in, so you just pick your account and press Send), you will receive instructions on your email on how to pay and later on with the 10-character code you get back — checked **entirely on-device** (no account, no sign-in, nothing leaves your car, works fully offline). Because the code is specific to your car, it can't be shared. Everything already in the app stays free; Pro only gates new premium additions.
- **Battery cell imbalance alert** *(Pro)* — an opt-in notification that fires when the live cell voltage spread (highest cell − lowest cell) stays above a configurable limit (default **50 mV**). It watches the same diagnostic as the Cell Voltage Spread heatmap, so a weak or failing cell can flag itself while you drive or charge rather than only when you go looking. To avoid false alarms it waits for the spread to hold over several consecutive readings, alerts once per episode (re-arming only after the spread recovers), and stays quiet below 5% and above 95% SoC — where a wider spread is normal (top-balancing near full, voltage sag near empty). Requires BYD Trip Stats Pro.
- **Tap-the-logo screenshot** *(Pro)* — tapping the **BYD logo** in the dashboard's top bar saves a PNG of the current screen to **Download/Screenshots/**, with a brief camera-style screen flash to confirm the capture. It saves the real composited frame (charts, gradients and the battery animation included), so you can quickly grab and share what's on screen. The flash plays only after the frame is captured, so it never appears in the saved image. Requires BYD Trip Stats Pro.
- **Battery health report** *(Pro)* — export a shareable summary of your battery's health from **Battery Degradation → Battery health report**, as a print-ready **PDF** or as **HTML** (opens in any browser). It captures current State of Health, the number of trips analysed, the decline rate, the projected date to 80%, the measurement period and the full recorded SoH history, plus a warranty reference — useful evidence when selling the car or raising a warranty claim. Generated entirely on-device and saved to **Download/BydTripStats/**; nothing leaves your car. Requires BYD Trip Stats Pro.
- **Back up to SD card** *(Pro)* — Settings → *Database Backup & Restore* → **Backup to SD card** saves the trip database to a `BydTripStats` folder at the root of a removable SD card. One tap — nothing to pick or configure; it checks there's enough free space first. Unlike the Download backup, SD backups **survive a factory reset or firmware downgrade** — off-device redundancy you can physically pull out and read on a computer. They appear in the "Available backups" list (tagged *SD card*) and restore/delete like any other. Requires BYD Trip Stats Pro.
- **"Keep this trip?" prompt before an auto-stop** (Settings → Preferences → *Ask before auto-stopping*, on by default) — if you're parked with the app open and the car-off timeout is about to end the trip (e.g. you stopped to take a call), the app now asks whether to **keep recording** or **stop**, instead of silently ending the trip and resetting the live range projection. While the prompt is up the trip is held open, so it won't slip away while you decide. A kept trip ends as soon as you drive again, tap **Stop**, or after ~30 minutes parked. This only ever engages while the app is in the foreground — park-and-leave still auto-stops exactly as before — and you can turn it off in Settings.

### Changed

- **Battery degradation chart now hides the old under-reported SoH era automatically** — before v2.4.0 the app *calculated* State of Health in a way that read low, which left a misleading dip-and-recover in the trend (e.g. 98% → 95% → 98%). Trips recorded before that switch are now excluded from the chart, trend line, 80% projection and the battery health report by default, so the line reflects your battery's real history. A new **Battery data range** control (Battery Degradation → *Adjust data range*) lets you change this: it only affects what's shown and **never deletes any data**, every change can be undone, and re-including the old readings asks for confirmation first. (Replaces the previous, easy-to-mis-tap "data baseline" reset, which could wipe the chart's history view with a single tap.)
- **Battery Degradation summary cards** — the "Lowest recorded" card just mirrored "Current SoH" on a steadily degrading pack, so it's been replaced with **"Trips analysed"** (how many trips back the trend), giving four cards that each tell you something distinct. The battery health report does the same.
- **"Always On" is now the default background mode** — to keep parked telemetry, off-state (e.g. overnight) charging detection, and the Web Companion / MQTT working while the car is parked, the background service now defaults to **Always On** instead of Minimal. Any explicit mode you already chose is kept; only installs that never picked one change over. You can still switch to **Minimal** or **Deep Sleep** under Settings → *Background activity*.
- **Settings → Preferences reorganised** — the settings are now grouped under clear headings (**General**, **Trips**, **Battery & telemetry**, **Costs**, **Power & background**) with the **BYD Trip Stats Pro** card pinned at the top, instead of one long unsorted list. Units now sits next to Theme, trip settings are together, and so on — easier to scan as the list has grown.
- **Longer stops no longer split a drive into a new segment** — the live Distance metric shows `segment (cumulative)`, and a fresh segment used to begin after the car had been off for just 90 seconds. That window is now **5 minutes**, so a long red light, a traffic-control wait, or a brief stop in **P** keeps the drive as one continuous segment instead of starting a new one. (Stops where you stay in **D** never split the segment at all, regardless of length.) This is purely about the segment display — it doesn't change when a trip ends or the cumulative total.

### Fixed

- **Energy Consumption chart flatlining on longer trips** — both the overview and the detailed Energy chart could show a nearly flat line near zero on longer drives. The trip was being summarised down to a handful of points *before* the energy curve was calculated, and that calculation ignores gaps longer than a minute — so on a long trip almost every summarised interval was discarded and the curve never climbed. Energy is now integrated at full resolution first and the finished curve is summarised for display, so it reads correctly regardless of trip length, and the end-of-trip total is always kept.
- **Speed & Power overview charts smoothing away brief peaks and dips** — the condensed Speed and Power charts now keep both the highest and lowest value within each summarised slice, so a short full-throttle burst, a hard regen spike or a brief stop no longer disappears into a single averaged sample. Peaks also now sit at the moment they actually happened instead of drifting toward the middle of the slice.
- **Projected-range line drawn flat when you open the app part-way through a drive** — if the app wasn't open for the whole drive (you got in, drove, then opened it — or reopened after a back press), the projection curve was rebuilt by applying a single trip-average consumption rate to every past point. Since your charge barely moves over a short drive, that rendered as a near-flat line parallel to the axis, throwing away the shape the line would have had live. The curve is now rebuilt point-by-point using the consumption rate that was current *at each point* (the same rolling-window calculation the live chart uses), so it reflects how your driving actually varied — motorway vs town, climbs, regen — instead of flattening. As a bonus, when the app *was* open during the drive the exact live curve is reused verbatim (kept in memory, and on disk so it survives a full restart). Display-only — trip recording, distance, energy and the live projection math are unchanged.
- **Projected range collapsing to about half once the live estimate kicked in** — a few hundred metres into a drive (or the moment you opened the app mid-drive) the projected-range line could drop to roughly half and sit there, flatter and far below the car's own estimate, even while you were driving efficiently — e.g. a Seal Excellence cruising at 15.8 kWh/100 km projecting as if it were using nearly double that. The live estimate measured its energy with a per-tick counter that, now that telemetry arrives ~10×/second while the battery's discharge total only refreshes about once a second, was counting the in-between moments twice — inflating the consumption it projected from. It now reads energy from the same battery-discharge total the trip's energy and consumption figures already use, so the projected line matches your real consumption (and the rest of the curve) instead of halving. EVs and PHEVs both benefit; trip recording, distance and energy totals were already correct and are unchanged.
- **PHEV projected range reading far too high (e.g. 87 km at 19% with the BMS showing 10 km)** — the 2.8.0 change made the projection electric-only, but on plug-in hybrids it could still balloon to several times the real EV range. The projection divides the remaining battery energy by your measured electric consumption (Wh/km), and on a PHEV that consumption is deflated whenever the petrol engine is driving or topping up the battery: kilometres keep adding up while the traction battery barely drains, so the measured rate collapses far below anything the car can actually do on electricity, inflating the range. The electric consumption used for the projection is now floored at the car's rated figure, so it can't drop to a physically impossible value — the floor only takes effect in that engine-assisted regime, while genuine electric driving (including driving more efficiently *or* less efficiently than rated) still shows through. On larger-battery plug-in hybrids the projection also now uses the car's own reported remaining electric energy when available — near a low charge the battery holds back a reserve for the engine, so "capacity × charge %" overstated what's actually left for electric driving. BEVs are unaffected.

---

## [2.8.0] - 2026-Jun-06

> **What's new in a nutshell**
>
> **Real-time speed, gear and power.** The dashboard's speed, gear and engine-power now update **instantly** (many times a second) instead of once a second — and gear is now accurate even while stationary. Also adds the **BYD Han EV** (and Han EV AWD) and fixes a handful of trip-tracking and Home Assistant / MQTT issues — including live telemetry that could freeze until a car off/on after an update, and a crash that could stop the background service from coming back up on its own. Fixed PHEV range projection.

### Added

- **Instant live telemetry — speed, gear, power & front motor RPM** — the dashboard speed, gear, engine power and front motor RPM now refresh in real time (≈10×/second) rather than the previous ~1-second cadence, for a smooth, responsive readout. Gear is now correct even at a standstill and while reversing (**R**), instead of being inferred from speed. (Rear motor RPM still updates about once a second — the head unit only reports it that fast.) Trip/database recording is unchanged (still sampled once per second), so this adds no database growth.
- **BYD Han EV & Han EV AWD** — full vehicle profiles (battery, kerb mass, WLTP, reference consumption, motor power, tyre pressures 2.5 bar front / 2.5 bar rear), selectable under **BYD Han** in the car picker alongside the existing Han DM-i.

### Fixed

- **App not restarting on its own after an update / car wake** — the background process-start helper could be removed by release-build optimisation, so after installing a new version the service didn't always come back up by itself until the app was opened manually (leaving MQTT and the Web Companion silent while parked). The helper is now preserved, so the car can bring the service back up automatically.
- **Live telemetry freezing until a car off/on — most often right after an update** — the vehicle data connection could stop delivering live events (speed, gear and power going silent or stuck at 0) when the app was restarted without cleanly detaching first, which is exactly what happens when a new version is installed over a running one. The app now releases the vehicle connection gracefully before an update so the new version starts on a clean channel, and if the live stream does stall it now notices and re-attaches on its own within seconds — no more switching the car off and on to get telemetry back.
- **Momentary SoC (BMS) flicker — a brief dip that instantly recovered** — the BMS occasionally reported a single isolated SoC sample that immediately reverted (e.g. 36.5% → 35.0% → 36.5% while driving). Real SoC changes far slower than that between readings, so an isolated jump is now held for one reading and discarded if it reverts; a genuine large change is delayed by at most one sample, so the readout no longer flickers.
- **Trip card status lagging the gear** — when parked with no trip yet, shifting **P → D** now shows **"Ready to Drive"** immediately. The trip card's gear letter and status text were updating on the once-a-second loop while the main gear indicator was already instant; they now share the same live gear.
- **Phantom charging animation while parked** — the battery could show the charging animation when nothing was plugged in, on cars whose BMS leaves its charger "gun/working" flags stuck on after a drive. The app now self-calibrates: if it ever sees those flags set *while driving* (which is physically impossible for a real charge), it stops trusting them on their own and requires actual charging power or a rising charge level to show charging. So a stuck flag no longer lights a phantom charging animation when you park, while genuine charging is still detected normally. Cars whose flags behave correctly are unaffected.
- **PHEV range projection always stuck at "≥ WLTP — capped at rated range"** — on plug-in hybrids the projected-range line added the petrol range on top of the electric projection, so with any fuel in the tank the total always blew past the electric WLTP ceiling and pinned at "capped," hiding the real comparison (e.g. a Sealion 6 DM-i at 65% and 19.6 kWh/100 km showed "≥ 92 km" instead of its true ~50 km electric range). The projection is now **electric-only** — it shows the EV range your actual consumption implies versus the BMS/WLTP estimate, matching what the SoC and consumption chart is about. BEVs are unaffected.
- **A new trip segment appearing mid-drive even though the engine never stopped** — a momentary gap in telemetry (a brief service hiccup while driving) was being mistaken for the car being switched off, splitting one continuous drive into two segments. A gap is now only treated as a car-off boundary when the odometer confirms the car genuinely wasn't moving across it.
- **Trip recording starting "from now" when the app is opened well into a drive** — opening the app 15–20 minutes into an already-moving trip started tracking (and live stats) from that moment instead of recovering the trip already in progress. The app now back-anchors to the in-progress journey on open, so distance and timing reflect the whole drive.
- **Wrong timestamps from an out-of-range ECU clock** — the head unit's reported datetime is now sanity-checked against the phone clock and ignored when it's implausibly far off, preventing skewed trip times.
- **Home Assistant entities stuck "Unavailable" despite MQTT data arriving** — after a WiFi drop the client reconnected internally and never re-published its "online" availability, so HA kept showing the last "offline" even while telemetry flowed into the broker. Availability is now re-published on every reconnect, and a failed publish forces a clean reconnect.
- **MQTT not publishing while parked in "Always On" mode** — in Always-On the parked poll loop had slowed to one tick every 5 minutes, so MQTT effectively went quiet between drives. Always-On now keeps a 30-second cadence while parked so telemetry keeps flowing to the broker.

---

## [2.7.0] - 2026-Jun-01

> **What's new in a nutshell**
>
> The new **Web Companion** serves your trip and charging history to any browser on the same WiFi — open the PIN-protected address shown in Settings and browse, sort, and filter everything live, with all charts and maps (always-on offstate mode needed). A new **Deep Sleep** parking mode lets the car's ECUs fully sleep, and you can now **favourite** trips and charging sessions to protect them from trimming. Plus a batch of sensor and trip-accuracy fixes (RPM, speed, average speed, PM2.5, range projection).

### Added

- **Web Companion** (Settings → App) — a local web server that serves the full trip-history app to any browser on the same WiFi. PIN login with 5-attempt lockout, configurable port (default 8888), one-tap copy of the address. Browse, sort, and filter all trips and charging sessions with the same 9 charts and maps as the app, live; also usable as a file-upload tool when offline.
- **Web Companion — mobile install** — can be added to the iOS home screen (Safari → Share → Add to Home Screen) or installed on Android Chrome as a standalone app.
- **Deep Sleep background mode** (Settings → Preferences) — the car-off toggle is now three-way: **Always On**, **Minimal**, and **Deep Sleep** (service fully stops so the car's ECUs can sleep). New installs default to Minimal; existing explicit choices are kept. Automatic trip recording is unaffected of which mode is active.
- **Keepalive health line** (Settings → Preferences, under the background-mode selector) — shows when Minimal's 90-minute keepalive last fired (and whether it's armed). On head units that fully power off at park the alarm can't fire, so a persistent "never fired" tells you Minimal behaves like Deep Sleep on your car and Always On is needed for parked data.
- **Favourite trips & charging sessions** — tap the ☆ on any trip or session to protect it from the manual "Trim database" action, keeping its full detail. Each history screen gets a ☆ favourites-only filter, and favourites also appear in the Web Companion (toggleable live over WiFi).

### Changed

- **Clearer off-state availability for Web Companion & MQTT** — the Settings descriptions now spell out that both run inside the telemetry service, so while the car is parked they are continuous only on **Always On**, intermittent on **Minimal** (reachable only during the ~90-min wake blips, off-state charging caught within ~90 min), and unavailable on **Deep Sleep** (off-state charging is reconstructed from the SoC delta on the next start rather than published live).

### Fixed

- **Speed, motor power and RPM reading 0 until a car restart (beta regression, now fixed)** — a device-initialization change introduced during the 2.7.0 beta cycle corrupted the vehicle data binding on a fresh app start, so speed/power/RPM/gear could freeze at 0 until the car was switched off and on once. It has been reverted: the data interface is now left exactly as the system hands it over, matching 2.6.0 and earlier (which never had the problem). As a safety net the app still **detects** the stuck-at-0 state (speed pinned at 0 while GPS shows you're moving) and shows a self-clearing banner suggesting a single car off/on — but in normal use you shouldn't see it, and upgrading 2.6.0 → 2.7.0 is unaffected.
- **Charging session ending below 100% (e.g. "→ 98.5%") on a full charge** — the end SoC was the last value sampled *while charge current was still flowing*, but the BMS keeps recomputing SoC upward to 100% during the CV/settle phase after current stops, and the session closed the instant charging ended. A near-full off-state session now keeps revising its end SoC (and energy) for a few minutes after the charge stops, while the car stays parked, so it reflects the true final charge. Capped and bounded so it can never absorb a drive or a separate charge; requires the service to stay alive through the settle (continuous on Always On).
- **Impossible average speeds (way higher than 200 km/h) on old trips, Duration showing "0m"** — trimming widened the gaps between stored points, and a duration recompute mistook those gaps for the car being off, collapsing the trip's duration. The recompute now rejects impossible results, and a one-time repair restores correct duration and avg speed on affected trips. Distance and SoC were never wrong.
- **Front motor RPM stuck at a flat ~8191 RPM** — that value (and similar) is a CAN "signal unavailable" sentinel that had slipped through; it's filtered again, in both the app and the stored-trip charts.
- **Speed briefly dropping to 0 km/h while driving** — the speed sensor occasionally returns a stale 0; the app now holds the last value unless power and GPS agree the car is actually stopped.
- **Range projection badge always showing "Baseline"** — the active model (Speed bins / Live trip) wasn't reaching the chart; it now updates correctly with distance driven.
- **Range projection subtitle wrongly saying "Low-speed calibration"** — when you're genuinely beating the WLTP cycle it now reads "Efficiency above WLTP average — capped at rated range"; the calibration wording is kept only for the baseline stage.
- **HV / 12V history chart ignoring the SoC source setting** — the SoC line and tooltip now follow your Panel/BMS choice instead of always showing BMS.
- **Route Analysis segments mislabelled "SoC (BMS)"** — the label now matches your selected SoC source.
- **PM2.5 not showing until app restart or AC cycle** — the first reading now displays immediately instead of waiting for a confirmation poll.
- **Average speed skyrocketing at the start of a trip** — stale firmware journey counters (distance present, time near zero) are now detected and discarded so the clock starts cleanly.
- **Average speed showing 200+ km/h right after opening the app mid-drive** — the trip clock is now back-dated to match the recovered distance.
- **Avg speed/consumption briefly wrong after stopping then immediately restarting a trip** — the timing and off-state anchors now reset together at the moment you press Start.
- **Charging icon stuck "in progress" after a PHEV reaches 100%** — a misleading end-of-charge power blip is now ignored on cars that report proper charging power.
- **Trip history showing `SoC (BMS) 0% → 0%` on PHEVs** — trips and charging sessions now store panel SoC too, and all screens respect your selected source.
- **Front/rear motor RPM cards flickering between 0 and a real value** — a spurious 0 while moving now keeps the last known RPM.
- **Update banner not showing when upgrading from a beta to the stable release** — beta suffixes are now handled correctly in the version comparison.
- **Wrong distance/avg speed for auto-detected trips after a manual stop** — a fresh service start no longer back-anchors to a stale journey counter, which had inflated short trips by the day's earlier mileage.
- **Switching to Minimal while already parked never caught off-state charging** — the off-state keepalive (which wakes the app to sample overnight charging in Minimal) was only (re)armed on a car off→on→off cycle, not when you picked the mode in Settings. Coming from Deep Sleep — which cancels that keepalive — and switching to Minimal without driving left no keepalive scheduled at all, so plugging in the charger produced no telemetry. Selecting a mode now immediately reconciles background scheduling (arm the keepalive for Minimal, cancel it and go resident for Always On, cancel it for Deep Sleep). Valid between beta versions - released won't have this issue.
- **Wrong resume metrics after engine off/on within a trip** (Minimal/Deep Sleep) — the trip now re-syncs from the database on resume so distance, energy, and consumption stay continuous.
- **Live segment distance frozen at "0.0" after a brief stop and restart** — the live Distance was computed purely from the odometer, which the car's mileage ECU reports late for several seconds after an engine restart, so the new segment stayed at 0 even while moving. The speed-integrated distance (which was being calculated but never shown) now bridges that gap, so Distance keeps moving the instant any speed reading is available and snaps back to the odometer once it ticks.
- **Speed frozen at 0 while driving (poll-only getter going stale)** — the dashboard speed came solely from polling the BYD `getCurrentSpeed` getter, which on some firmwares freezes at a stale 0 after an auto-started trip and only recovers on an engine off/on. The speed device's live **event stream** (the same mechanism that keeps motor RPM updating) was being received but only logged, never applied. Speed is now driven from that event stream — the app auto-learns the event-to-km/h scale while the getter still works, then keeps the speedometer alive from events when the getter stalls, falling back to the getter when events aren't flowing.
- **Gear stuck on "P" while driving** — on firmwares where the gearbox device never reports a gear (its `gearCode`/`currentGear` getters return NULL and no gear event fires), the gear stayed pinned to its default "P" even at speed, because the code trusted the gearbox source unconditionally and never fell back to the speed-based guess. Since gear=D/R is also used as a "car on" proxy, this could knock other readings off too. A physical-impossibility guard now overrides "P" to "D" whenever the car is clearly moving forward (>5 km/h), and a diagnostic was added to pinpoint whether the gearbox device is silent or actively misreporting.
- **Speed flickering to 0 mid-drive from the event stream overriding a healthy getter** — on firmwares where the speed event's raw value isn't proportional to km/h, the learned scale was meaningless and the event-derived value (which could land anywhere, including ~0) was overriding a perfectly good `getCurrentSpeed` reading whenever an event had arrived in the last 3 s — i.e. constantly while driving. The polled getter is now authoritative whenever it's healthy; the event stream only bridges a genuinely frozen getter, and only when GPS or traction power independently confirm the car is moving (so it can't invent speed at a standstill either).
- **Energy consumption over-reported (regen not credited)** — the per-tick energy accumulator summed only *positive* changes in the BYD consumption counter, which silently discarded every regen tick and reported **gross** energy instead of **net**. On trips with meaningful regen (e.g. a long descent) this over-stated consumption substantially. The accumulator now sums both signs (discharge minus regen) for true net consumption, while still dropping the counter's car-on reset so multi-segment trips stay correct. The stored trip energy also no longer prefers the running *peak* discharge (which sits above the end value once you regen past it).

---

## [2.6.0] - 2026-May-26

> **What's new in a nutshell**
>
> A reliability patch with PHEV-specific improvements. Trip energy consumption was being recorded at roughly half its real value on cars whose BMS counter resets at every engine-on cycle — a trip that spanned an engine-off pause merged into one entry in the app, but the saved kWh figure only reflected one of the segments. Consumption is now summed across all segments and matches what other apps and the car's own SoC drop show. State of charge could read 0% on some plug-in hybrids; SoC now reads from BYD's standard getters when the internal mapping path comes back empty. The regen mode indicator was showing the wrong value because it was reading the powertrain source (EV / Forced-EV / HEV) instead of the actual regen level, so Forced-EV displayed as "Higher" regen and EV as "Standard". The indicator now reads only the real regen signal, and hides itself entirely on firmwares that don't report a regen level at all. The trip-controls card no longer briefly shows triple-digit average speeds when recording is started manually while already driving; the displays now reset at the press moment instead of inheriting the back-anchored car-on values. Charging sessions on plug-in hybrids no longer stay stuck "in progress" forever — the stale-state guard now also covers fresh installs where no previous capacity activity was recorded in the current session. A new SoC source preference lets PHEV/BEV users choose between the instrument-panel reading and the BMS reading; panel is the default since BMS SoC is often unsupported on DM-i. A theme picker (Light / Dark / System default) is now available under Settings → Preferences. SoC values across the dashboard and route analysis now show one decimal place instead of being rounded to whole numbers. Mud and Sand drive-mode labels were swapped and are now correct.

### Added

- **Theme mode preference** (Settings → Preferences) — choose Light, Dark, or System default. System default (the previous hardcoded behaviour) follows the device's system-wide dark/light setting and remains the default.
- **SoC source preference** (Settings → Preferences) — choose whether the dashboard's battery percentage, battery gauge icons, and range projection chart use the instrument-panel SoC or the BMS SoC. Defaults to Panel. An informational note explains that BMS SoC is usually not reported on PHEVs — switch to Panel if BMS shows 0.

### Changed

- **SoC values now show one decimal place** — the SoC (Panel) / SoC (BMS) stat card, route-analysis start/end waypoint labels, and charging-session tooltips now display e.g. `73.4 %` instead of `73 %`. Chart axis tick labels remain whole numbers for readability.

### Fixed

- **Trip energy under-reported by ~half on cars whose BMS counter resets at engine-on** — the saved `kWh` for a trip is now the sum of positive per-tick changes in the BMS discharge counter, not the simple end-minus-start of the counter. On firmwares that reset `getTotalElecConValue` to ~0 at every engine-on cycle (notably the Seal Excellence AWD), a single trip that spanned an engine-off pause was previously capturing only one segment's worth of consumption — typically around half. The same fix also feeds the live "energy consumed" display on the dashboard so the running number matches what the trip will save. EVs with a lifetime-cumulative counter are unaffected — the sum is mathematically identical to end-minus-start when no reset happens.
- **State of charge reading 0% on some plug-in hybrids** — SoC is now read from BYD's standard `getElecPercentageValue` / `getSOCBatteryPercentage` getters when the internal getter mapping returns nothing (or a zero), instead of failing silently. Fully-charged EVs are unaffected — the fallback only engages when the primary path yields no usable reading. A second guard now also rejects bad SoC values arriving via the SDK's event channel (null / sentinel / out-of-range), which would previously overwrite a good polled value and snap the dashboard to 0.
- **Regen mode indicator showing the wrong level** — the indicator was driven by the powertrain energy mode (EV / Forced-EV / HEV), so switching to Forced-EV wrongly showed "Higher" regen and EV showed "Standard". Regen level is now sourced only from the genuine regen signal.
- **Regen indicator on cars that don't report a regen level** — the Driving Dynamics card no longer shows "UNKNOWN" or nags to "change regen modes" on firmwares (notably DM-i PHEVs) that never expose a regen level; it shows only the drive mode in that case.
- **Triple-digit "AVG" briefly shown when recording is started manually mid-drive** — pressing Record while already moving (auto recording off) could flash an average speed of several hundred km/h for a few seconds before the trip-anchored values took over. The trip-controls card now resets distance / time / segment immediately at the press moment so the AVG starts from zero and climbs to the real value as the trip accumulates, instead of inheriting the pre-trip back-anchored journey distance divided by a brand-new elapsed timer.
- **Charging session stuck "in progress" on plug-in hybrids — including after a fresh install** — on some DM-i firmwares the BMS never returns its `chargerWorkState` to idle after charging finishes. The existing ten-minute stale-state safety net previously only fired if capacity activity had been observed in the current session, so a fresh install connecting to a car with a stuck work state would never trigger it. The guard now also activates based on how long `chargerWorkState` has been non-zero without any corresponding power or capacity data, covering the fresh-install case.
- **PM2.5 showing `0` on cars without an interior air-quality sensor** — vehicles that don't have a PM2.5 sensor return `0` from the SDK, which was previously passed through and displayed as `0/— μg/m³`. The reading is now suppressed for values of `0` since a genuine PM2.5 level of zero is not physically measurable by in-cabin sensors; those cars will show no PM2.5 subtitle on the Environment card instead.
- **PM2.5 not updating after initial connection on some PHEVs** — PM2.5 was read once at climate-device registration but never again; on PHEV firmware the debounce requires two consecutive identical readings to confirm a value, so the single initial read left the reading permanently pending. PM2.5 is now re-polled from the climate device every 5 seconds so the debounce can confirm.
- **Range metric showing 0 or the fuel driving range on PHEVs** — the range projection used `soc` (BMS) to compute remaining energy; when BMS SoC briefly reads 0 at engine wake-up the projection collapsed to 0 or, on PHEVs, snapped to the fuel-only range (e.g. 900 km). SoC now falls back to the panel reading when BMS reports 0. Additionally, the Range stat card on PHEVs now shows the car's own EV-only range estimate instead of the trip projection (which included the fuel offset and produced misleadingly large numbers).
- **Mud / Sand drive-mode labels were swapped** — corrected to match BYD's mode constants.

## [2.5.0] - 2026-May-20

> **What's new in a nutshell**
>
> A reliability-and-features release. Battery temperature is back on the Battery card and now matches what other BYD apps on your car report — the earlier decoder was wrong by a few degrees because it was guessing between two possible encodings, and now uses BYD's actual `raw − 40` formula directly. The range projection finally calibrates to your real driving consumption within the first kilometer, instead of getting stuck on the catalog baseline for the whole trip. Charging sessions correctly close when you unplug and drive away, even on firmwares that don't fire the unplug callback reliably. Six new cars are supported (Seal U DM-i Comfort / Design AWD, Sealion 6 DM-i Premium / Performance AWD, Sealion 6 EV Standard / Extended). The range projection chart got a holographic redesign with a confidence cone around the BMS estimate and per-segment colouring, and the dashboard top bar reshuffle frees vertical space (optional) so the chart can breathe. Trips can now be exported with every telemetry field captured during the drive, sent straight to your configured Telegram bot, or saved as a single self-contained `.html` file that opens with one double-click in any browser — no separate JSON to manage, no viewer to host, all charts pre-rendered on open, locally at your device. ABRP users save ~98% on data with the new car-on/charging upload gate and HTTPS connection reuse, and Telegram database backups now check the 50 MB bot limit before uploading.

### Added

- **Configurable engine-off trip timeout** (Settings → Preferences) — previously the trip recording window after the engine turns off was hardcoded to 30 minutes. This is now a user-settable value: if the car comes back on within the window the recording resumes seamlessly (same trip, new segment, cumulative distance); past the window the trip ends and the next drive starts a new one. Default remains 30 minutes.
- **Minimum trip distance filter** (Settings → Preferences) — trips shorter than the configured threshold are automatically discarded at the moment they end: the trip entity, data points, route segments, and stats are all removed, keeping the database free of driveway nudges and accidental short moves. Set to 0 to keep every trip (default). Note that discarded trips are absent from all derived views — history list, consumption charts, monthly totals, seasonal analysis, and the SoH degradation series. The consumption chart already ignores trips under 0.5 km regardless of this setting, so only thresholds above 0.5 km affect the chart.
- **Battery temperature is back on the Battery card** — the stat card now shows the current battery temperature as a subtitle beneath the SoH percentage. This was removed in 2.3.0 because some cars were reporting wildly incorrect values; it's back now that the root cause has been fixed.
- **Tyres are now a dedicated stat card when animations/icons are disabled** — FL / FR / RL / RR temperatures and pressures live in their own card on the right-side stats panel in a 2×2 layout, replacing the old tyre icon in the top bar. The pressure unit (Bar / PSI / kPa) is still tappable to switch.
- **BYD Seal U DM-i Comfort** — 27.3 kWh Blade LFP (144S est.), 26.6 kWh usable, FWD 145 kW, 2,210 kg, 125 km EV-only WLTP.
- **BYD Seal U DM-i Design AWD** — 18.3 kWh Blade LFP (96S), 15.2 kWh usable, AWD (150 kW front + 120 kW rear), 2,100 kg, 70 km EV-only WLTP.
- **BYD Sealion 6 DM-i Premium** — 18.3 kWh Blade LFP (96S), 15.2 kWh usable, FWD 160 kW, 1,880 kg, 92 km EV-only WLTP.
- **BYD Sealion 6 DM-i Performance AWD** — 18.3 kWh Blade LFP (96S), 15.2 kWh usable, AWD (160 kW front + 150 kW rear), 2,010 kg, 80 km EV-only WLTP.
- **BYD Sealion 6 EV Standard** — 71.8 kWh Blade LFP (132S), FWD 150 kW, ~430 km WLTP.
- **BYD Sealion 6 EV Extended** — 87.04 kWh Blade LFP (160S est.), FWD 150 kW, ~510 km WLTP.
- **Trip export now carries every telemetry field** — CSV and JSON exports of a trip's data points now include all ~30 fields the app records per point. Previously only ~10–12 were emitted (timestamp, GPS, speed, power, SOC, gear, motor RPM). New fields include odometer, battery temperature, total discharge, regen flag, BMS estimated range, all four tyre pressures and temperatures, SOH, pack voltage, 12 V voltage, min/max cell voltages, panel SOC, and the raw telemetry blob. The new JSON exporter also filters out data points captured before the GPS fix arrived (lat=0, lon=0) so the file is route-clean.
- **Send a trip's CSV, JSON, or HTML viewer straight to your Telegram bot** — Trip Detail → Export now has three "Send to Telegram" buttons that ship the export to your configured backup bot, reusing the same file-send path as database backups and the vehicle-compatibility probe. The buttons are disabled until Telegram is set up under Settings → Backup & Restore.
- **HTML trip viewer** (`docs/trip-viewer.html`) — a self-contained, no-server, browser-based viewer that accepts an exported trip JSON (file picker or drag-and-drop) and renders ~15 visualisations: GPS route on an OpenStreetMap basemap, speed/power/SOC/altitude/RPM time series, plus derived charts (cumulative distance, cumulative consumed-vs-regen energy, speed-vs-power efficiency cloud, power-distribution histogram), plus optional charts for any of the new telemetry fields the export carries (battery temp, tyre pressures and temps, voltages, SOH, etc.). All processing happens locally in the browser; nothing is uploaded.
- **"Save as HTML viewer" trip export** — the same viewer, but with the trip's data already embedded inside it. Trip Detail → Export → "Save as HTML viewer" produces a single `.html` file in Downloads that you can double-click on any device with a browser — no JSON to manage, no viewer to host, no install. The viewer template is bundled into the app at build time (`docs/trip-viewer.html` → `assets/`) and the trip JSON is injected as `window.__embeddedTrip` before the viewer's main script runs, so the file picker is skipped and all charts render automatically on open. A "Send HTML viewer to Telegram" button mirrors the file-save path. Typical drive produces a ~500 KB self-contained `.html`.

### Changed

- **Range projection chart redesign (Holographic Sweep)** — the projected-vs-BMS chart now uses a phosphor / holographic look: dot-grid plot field, a translucent confidence cone that widens with trip progress around the BMS line, a neon multi-pass projected stroke, and a "tape-measure" delta callout at the leading edge between the two curves. The empty/calibration state replaces "No trip data yet" with corner brackets and a "SCANNING…" callout. Theme-aware: dark mode shows the full neon panel; light mode degrades gracefully (no dark panel, no halo glow, theme primary instead of cyan) so it blends with the lighter surface. Color semantics preserved — green when projected ≥ BMS, amber when worse.
- **Dashboard top bar redesign** — the battery, AWD/axle, and consumption-chart icons that previously sat in a 110 dp strip above the range projection have been moved into the top app bar (left of the History icon). The freed vertical space gives the range projection chart room to breathe. The consumption icon is tinted green to distinguish it from the History icon, and all four top-bar actions (battery → consumption → history → settings) are evenly spaced.
- **Car selection is now collapsible by category** — the Battery Electric and Plug-in Hybrid groups on the Initialization screen and the Dashboard's "Change car" dialog now each expand/collapse independently with a chevron. BEV opens by default; PHEV opens only if your selected car lives there.
- **Projected range chart no longer flips** — the 3D flip animation that revealed alternate views of the projection chart has been removed. The chart is now substantially taller (it inherits the space freed by the top-bar redesign above), so the flip affordance no longer earns its keep.
- **"Dashboard icons & animations" preference** (Settings → Preferences, **default on**) — repurposes the old animations toggle into a single switch that decides which dashboard layout you get. On (default) restores the pre-2.4.1 layout: liquid-fill battery icon, animated energy-flow lightning bolt, AWD/axle drawing with four tyre pressure/temperature overlays, and a consumption thumbnail — all rendered in a row above the range projection chart. Off keeps the new 2.4.1 layout: those elements move into the top app bar (battery + consumption icons) and a dedicated Tyres stat card on the side panel, freeing the range chart's full height and disabling animations. The chart flip animation stays off in both modes.
- **Consumption legend wording** — "Selected duration avg" → "Your duration avg" in the expanded consumption chart legend, to make it clearer that the line reflects the user's own driving over the selected period.
- **ABRP default upload interval raised from 5 s to 30 s** — Settings → Connections → ABRP. The 5–120 s range stays available for users who want a tighter cadence; only the default for new installs (and existing users who never touched it) moves. Combined with the new car-on/charging gate below, this drops worst-case ABRP data usage from ~140 MB/day to ~5 MB/day for heavy drivers and to near-zero on idle parked days.

### Fixed

- **Live "energy consumed" no longer spikes to thousands of kWh at trip start** — and now stays in step with the trip overview's stored value (which is what reference apps like Electro report for the same drive). The live Trip Tracking card reads the BMS `totalDischarge` counter delta against the *corrected* trip-start anchor exposed by `TripRepository.currentTripStartTotalDischarge()`. The repository runs a per-tick self-correction (see [TripRepository.kt](app/src/main/java/com/byd/tripstats/data/repository/TripRepository.kt)) the moment the BMS produces a plausible reading after a stale `0` at trip start, so the live delta picks up the corrected anchor automatically instead of carrying the multi-thousand-kWh spike forward. As defence-in-depth for the brief window before the correction fires (or if the override path supplies an unsane override), the displayed value is clamped to the physical envelope `[0, 2 × pack capacity]` and falls back to the power-integrated traction estimate outside it.
- **Range projection now reports a realistic value from the first kilometre instead of being pinned in `● Baseline`** — on Seal Excellence (and any car with a sparse or unreliable engine-power signal) the projection used to sit on the catalog baseline for the entire drive, sometimes underestimating range by half. Several things changed together: (a) the rolling-window energy accumulator and the speed-bin accumulator both feed from the BMS `totalDischarge` delta — the same source the CONS readout uses — with engine-power only as a fallback, both during a live drive and when restoring an in-progress trip from disk; (b) the speed-bin accumulator counts distance every tick, with energy added only when positive, so sparse BMS updates can't starve the bin distance below `BIN_MIN_DIST_KM`; (c) the `● Historical bins` tier averages Wh/km across *all* populated speed bins weighted by distance, and — crucially — falls back to the trip-cumulative consumption (the exact figure the CONS readout shows: trip-total kWh ÷ trip distance) whenever the speed bins themselves haven't accumulated usable energy. That trip-cumulative path always resolves once the drive has covered ≥ 0.2 km and consumed anything, so the projection leaves the catalog baseline regardless of how erratically the firmware increments its discharge counter; (d) the model badge is picked across all three tiers (`● Live trip` / `● Speed bins` / `● Baseline`) when the app is reopened mid-trip instead of skipping `● Speed bins` entirely; (e) the orange projection line is drawn from km 0 in the restored chart rather than leaving a 3 km gap before the old stabilisation threshold. End result on a typical city drive: the badge flips from `● Baseline` to `● Speed bins` within ~0.2 km, then to `● Live trip` at ~3 km, and the projection converges to roughly `(remaining kWh × 100) / CONS` from the first kilometre instead of after 30+ km of driving.
- **Range projection chart polish** — the projected orange line now starts at km 0 instead of waiting for the first stabilised point (was a small but visible gap near the y-axis); the y-axis stays well-formed on rare cold-boot ticks where the BMS hasn't reported a range yet at trip start (could previously degenerate to a zero-height chart); and saturated projections (above the WLTP cap, i.e. calibration not yet settled) now render as a soft asymptotic bulge into the top padding rather than a hard line clipped at the chart's ceiling, so they're visible regardless of starting SoC.
- **Live Distance metric now shows the new segment alongside cumulative trip distance after an engine-off resume** — e.g. `13.3 (17.8)` once you've driven 13.3 km in the post-resume segment of a 17.8 km trip. Previously the segment anchor was set from the BYD `currentJourneyDriveMileage` counter, which on some firmwares doesn't reset across a brief engine-off cycle — so segment ended up equal to cumulative and the parentheses never appeared. The off→on transition handler now anchors the segment at the current odometer directly, and the same correction is applied on app-restart restore by scanning the stored data points for the most recent off-state gap.
- **Range projection chart now colours each segment by its own projected-vs-BMS verdict** — the orange/green line and shaded area used to take a single colour based only on where the *current* projection sat relative to BMS, repainting the entire trip history in that colour. Each segment between consecutive sampled points is now classified independently: above the BMS line it renders green (you outperformed expectation across that stretch), below it renders amber, with the shaded area beneath the line matching segment by segment. The duplicate "Δ" label that used to float at the leading edge inside the chart is gone too — only the chip in the top-right of the chart frame and the existing vertical tape-measure line + tick caps remain as the delta indicator.
- **Stored trip energy is now resistant to a stale `totalDischarge` anchor** — `TripRepository` now (a) re-anchors a trip's `startTotalDischarge` on the fly the moment the BMS produces a reading that implies the original anchor was a stale zero (observed delta exceeds both 2 × pack capacity and the power-integrated estimate of energy actually drawn so far), back-computing the corrected anchor from the integrated draw, (b) clamps the final `(endTotalDischarge − startTotalDischarge)` to the power-integrated estimate at trip-close time if the reported delta is still physically impossible, and (c) applies the same plausibility clamp at each 30 s segment flush, using a per-segment slice of the trip's power-integrated draw as the fallback (a 30 s segment can't physically consume more than ~2 kWh; anything above 50% of pack capacity is unambiguously corrupt). Together these protect the stored `energyConsumed`, the per-segment `energyUsed`, and every downstream chart, consumption stat, monthly total, and seasonal analysis that depends on them against the same stale-zero failure mode that was corrupting the live display.
- **Battery temperature now shows a consistent, accurate value across all screens** — the Dashboard stat card, Trip Detail average, and Trip Detail min/max range now all agree with each other and reflect actual battery cell temperature (not a coolant-side probe that reads several °C lower).
- **Battery temperature decoder rewritten to match BYD's actual encoding** — earlier 2.4.1 builds tried to guess between two possible encodings (direct °C vs ÷3) using anchors and ambient as tiebreakers, and got it consistently wrong by a few degrees against reference apps (Electro, the open-source Overdrive). The decoder now uses BYD's documented `raw − 40 = °C` formula directly, with raw values validated to be in `0..120` (i.e. −40°C to 80°C). Result: cell temperatures match what every other BYD-aware app on your car reports, instead of being 4–5°C off in either direction.
- **Battery cell temp sentinels no longer clobber the last good reading** — the BMS-uninitialized value 0 and BYD's INVALID/UNAVAILABLE sentinels are filtered out so quiet polls don't overwrite a real temperature with a placeholder.
- **Old trips with bad stored temperature averages now display sensibly** — for trips recorded before this fix, the average battery temperature shown in Trip Detail is now re-derived from the min/max cell range rather than the previously stored (potentially wrong) average.
- **Battery temperature max is now shown in Trip Detail even when the car doesn't report it directly** — the max is estimated from the min and average, so you'll see a range like "20°C – 22°C" instead of just "20°C".
- **Trip export dialog sections are now collapsible** — "Save to Download folder" and "Send to Telegram bot" each have a tappable header with a chevron and start collapsed. The dialog now opens with just the Clipboard button and two section headers, so users can see the two groups exist at a glance and tap into whichever they want, instead of being faced with seven buttons up-front. The previously hidden third Telegram button ("Send HTML viewer to Telegram") is now discoverable inside the expanded section.
- **Trip duration and average speed no longer count engine-off resume time** — when you stop the engine and the trip stays open inside the configurable engine-off resume window (Settings → Preferences), the wallclock keeps ticking. Until now that time was getting added into the saved trip's duration, which made average speed read low (e.g. a 30 km trip you actually drove in 45 min would average ~20 km/h instead of ~40 km/h if the trip closed 45 min after the engine went off because the resume window was 30 min and you parked overnight). The stored duration and avg speed now reflect actual driving time only — gaps in the recorded data points where the car was off are subtracted. Existing trips are back-filled automatically on first launch after the upgrade (one-shot pass over every completed trip), and the live Trip Tracking card's TIME / AVG also subtract any mid-trip engine-off windows in real time. Schema bumped v3 → v4 to add the per-trip stored value.
- **Gear indicator no longer says "P / Trip in progress" when the car auto-parks at a stop** — BYD automatically shifts to P at a standstill (e.g. in traffic), which previously caused the gear circle to read "P" and the status to read "Trip in progress" — misleading, since the user hasn't actually parked. The gear circle now shows "·" in a muted colour and the status reads "Stopped" while a trip is active and the car has auto-shifted to P. A manual P (no active trip) still displays as "P" and "Waiting for Trip…".
- **Charging session no longer stays "in progress" after you unplug and drive away** — some firmwares don't fire the unplug callback when the plug is removed while the car is locked (most common: charge overnight, walk up in the morning, unplug, drive off). The plug-presence flag stayed stuck non-zero, so the app kept showing "charging" through the next drive and the overnight session in history stayed open. The "is charging now" signal now uses the BMS-level charger work state (which clears once the BMS actually finishes a session) as its primary input, with a defence-in-depth override that force-clears the charging state the moment the car is genuinely driving (speed > 5 km/h in D or R) — physically impossible to be charging at that point. On install, any session left open from before the fix is auto-closed on the next trip start with its end-of-data-points timestamp and final SoC.
- **MCU WiFi keepalive during charging now triggers on all three charging signals** — to prevent the car cutting WiFi ~15 minutes after ACC_OFF mid-charge, the periodic keepalive ping now fires whenever the BMS reports active charging, an active charger work state, *or* the plug being physically in the port. Earlier 2.4.1 builds used only the plug-presence signal; if the plug callback misfired, the keepalive missed and the app could lose telemetry mid-charge.
- **Telegram auto-backup no longer wastes data on oversized DBs** — the periodic Telegram backup worker (and any manual "Send to Telegram" tap) used to upload the entire database file before the Telegram Bot API rejected it with "Request Entity Too Large" — Telegram bots cap at 50 MB. A heavy user with a 100 MB DB on a daily schedule could push close to 500 MB of wasted traffic over five days. The worker now checks the DB size *before* the multipart upload starts, skips the run when the DB exceeds 50 MB, and surfaces a clear message ("Backup file is X MB — Telegram bots can only accept files up to 50 MB. Upload skipped to avoid wasting your data plan."). The local-backup path is unaffected.
- **ABRP uploads now pause when the car is parked and not charging** — previously the ABRP integration fired its upload loop from the foreground telemetry service whenever it was alive, which (with "Keep service alive when car is off" defaulting to true since 2.4.0) meant uploads continued 24/7 even though nothing meaningful about the car was changing. ABRP cares only about driving/charging telemetry, so the new gate (`!isCarOn && !isCharging` → skip) removes ~80% of the requests without losing any data that ABRP would actually use.
- **ABRP no longer reopens a fresh TLS connection on every upload** — `HttpURLConnection.disconnect()` was being called after every request, which forced the underlying TCP/TLS socket closed and disabled Android's HTTP connection-pool reuse. With ~17 k requests/day in the worst case, that was paying a ~5–8 KB TLS handshake on every single one. The success path now drains the response body and lets the pool keep the socket alive; disconnect still runs on the IOException path so a half-broken connection doesn't poison the pool. Saves ~9× bytes per request once the connection is warm.

---

## [2.4.0] - 2026-May-13

> **What's new in a nutshell**
>
> This release is primarily a reliability and polish update. The biggest change is that the app now stays connected by default while the car is off — this means the battery history chart no longer has gaps, and charging sessions that happen while you're away from the car are now captured properly (including real-time data points and correct kWh totals). UK and other Imperial users get a comprehensive fix pass: speed, distance, and consumption values are now correctly converted throughout all charts and screens, not just the summary cards. There's also a "Check for updates" button if you don't want to wait for the next app restart, and trip stats are displayed larger when the app is in full-screen landscape.

### Added

- **"Keep service alive when car is off" toggle** (Settings → Preferences, **default on**) — when enabled, the telemetry service no longer self-stops after the car has been off for 5 minutes. The benefits: the 48-hour HV/12V battery history chart receives continuous samples instead of long gaps; ADB-over-WiFi stays reachable without having to remote-wake the car first; trip-tracking responsiveness is unaffected by off-cycle restart latency. Real-world measurement since v2.3.0 shows the dominant overnight drain comes from stock BYD services (the GPS provider, Telenav ISA/ARP, and telematics heartbeat), not from BydTripStats. The v2.3.0 self-stop + off-state keepalive + periodic-restart-cancel code paths are all retained behind this toggle for users who specifically prefer the lower-churn behaviour.
- **Persistent diagnostic log** at `/sdcard/Android/data/com.byd.tripstats/files/diag.log` (caps at 256 KB + 256 KB rolled `.prev`). Captures BootReceiver actions, off-state keepalive schedule/fire/cancel, service onCreate/onDestroy/self-stop, and Application.onCreate with PID. Pullable any time via `adb pull` — useful for debugging service-lifecycle issues over windows much longer than the logcat ring buffer (which is only 256 KB on DiLink and gets shredded in ~7 minutes by the system GPS provider's 4 Hz wake-lock spam).

### Changed

- **Default off-state behaviour reverted to "always alive"** — see the toggle entry above. v2.3.0 introduced the self-stop based on the assumption that the app was a meaningful contributor to overnight HV drain. The uninstall test (drain rate identical with the app uninstalled) refuted that assumption. Continuous sampling is the more useful default; the self-stop remains available for users who want it.
- **Distance display and recorded trip distance now track the main odometer delta exclusively** — previously the live PowerMetric distance and stored trip distance were computed as `max(journeyMileage, odometerDelta, speedIntegration)`, which caused the app to show ~0.4 km more than the instrument cluster odometer over a 16 km trip. Both the live display and the finalised trip now use the SDK main odometer delta as the sole source of truth; `journeyMileage` and speed integration are retained only as fallbacks for the edge case where no odometer anchor is available (e.g. app connects mid-journey on a cold boot).

### Fixed

- **Off-state keepalive now armed defensively from the service itself** — previously the 90-minute off-state keepalive chain was scheduled only by `BootReceiver` on `ACC_OFF` / `ACC_MODE_CHANGED`. Investigation showed that the DiLink firmware does not reliably broadcast `ACC_OFF` on every car-off transition (e.g. remote-off, sleep timeouts), so the chain was never armed and the alarm fired zero times across 22 days of system uptime. The service now also schedules the keepalive directly from inside its self-stop path — by the time `carOff && !charging` for 5 minutes has been observed from real telemetry, we are independently certain the car is off and don't need a broadcast to confirm it. Only relevant when the new "keep service alive" toggle is disabled.
- **Range projection chart header now respects the selected unit system** — UK / Imperial users had a chart Y-axis correctly labelled "mi" while the header text underneath still read "447 km projected range" / "+196 km vs BMS estimate", and the WLTP saturation label said "≥ X km projected". The BMS-sourced figures already arrive in the car's native unit (UK BMSes report miles natively), so the values were correct; only the unit suffix needed to flip. The WLTP figure comes from the catalog (always metric) and is now properly converted to miles for Imperial display.
- **Imperial unit label fixes across multiple screens** — several consumption, distance and speed labels throughout the app (route analysis tab, trip charts, settings goals & personal bests summary) were still displaying metric suffixes regardless of the selected unit system. All affected values are now converted and labelled correctly for Imperial users.
- **Speed and Distance PowerMetric values now correctly converted for Imperial users** — the Speed tile was showing km/h values labelled "mph" (e.g. 32 instead of 20), and the Distance tile was showing km values labelled "mi" (e.g. 12.6 instead of 8). Both are now converted. The Range tile is unaffected — BMS-sourced range values already arrive in the car's native unit (miles for UK BMSes).
- **Live trip stats larger in full-screen landscape** — the TIME / AVG SPEED / ENERGY USED / CONSUMPTION readouts in the Trip Tracking card are rendered at a larger type scale when the app occupies the full screen, making them easier to read at a glance while driving.
- **Manual "Check for updates" button** (Settings → About & FAQ) — a "Check now" button has been added to the update status card so you can re-trigger the GitHub release check at any time, without waiting for the next app restart. The card shows a spinner and "Checking for updates…" while the request is in flight.
- **BYD Seal U DM-i Comfort** — 27.3 kWh Blade LFP (144S est.), 26.6 kWh usable, FWD 145 kW, 2,210 kg, 125 km EV-only WLTP.
- **BYD Seal U DM-i Design AWD** — 18.3 kWh Blade LFP (96S), 15.2 kWh usable, AWD (150 kW front + 120 kW rear), 2,100 kg, 70 km EV-only WLTP.
- **Motor card kW now displays correctly on cars that don't report RPM** — the kW subtitle in the right-panel motor card was gated on the RPM sensors being active; on BYD variants where `engineSpeedFront`/`engineSpeedRear` are not populated the card always showed "0 kW" even while driving. Power is now read directly from `enginePower` regardless of RPM availability, matching the behaviour of the left-panel Power metric.
- **Charging session reconstruction now fires on every car wake-up** — previously the SoC-delta reconstruction check (used to create a synthetic charging session for car-off charging periods) ran only once per *service lifetime*. With "Keep service alive when car is off" enabled, the service stays running through the entire off-state charge, so the reconstruction guard was already consumed when the car woke up and no session was created. The guard is now reset on every off→on transition, ensuring reconstruction is attempted every time the car wakes from a possible charging period.
- **Charging session reconstruction survives mid-reconstruction process restarts** — the pre-charge SoC baseline is now written to disk *after* reconstruction completes on the first car-on packet of each wake cycle, rather than immediately before the coroutine runs. Previously, if the process was killed between the baseline update and the DB insert (e.g. an app update installed right as the car woke up), the next start would read the already-updated post-charge SoC, see a delta of 0%, and silently skip reconstruction.
- **MCU WiFi keepalive now uses BMS-level charging gun state** — the 10-minute MCU keepalive (which prevents the car from cutting WiFi ~15 min after ACC_OFF) was previously only sent when the SDK explicitly reported `isCharging=true`. Direct power readings go stale in deep sleep even when charging is physically active, so the keepalive was never sent precisely when it mattered most. It now also triggers when the charging gun is physically present (`chargingGunState` from `AbsBYDAutoChargingListener`, a BMS-level callback that fires independently of ignition). This covers pre-charge negotiation, active charging, scheduled-charge wait, and post-charge while still plugged in — and clears automatically on unplug, so normal unconnected overnight parking is unaffected. `chargerWorkState` was evaluated and rejected as the keepalive signal: its `FINISH` and `TERMINATE` values are non-zero but indicate charging has already ended, which would have kept WiFi alive unnecessarily after a completed session.
- **Off-state charging now detected via charging gun presence** — the `isCharging` flag used by the telemetry loop to select the 30 s poll interval was previously derived solely from direct power readings, which require a fresh SDK event (< 4 s old) and go absent when the car is in deep sleep. The detection now also checks `chargingGunState` from `AbsBYDAutoChargingListener` — a physical signal that is non-zero whenever the gun is in the port, regardless of ignition or charging phase. This ensures the service correctly identifies an off-state charging session, switches to 30 s polling, and records real-time data points throughout rather than falling back to 5-minute ticks.


---

## [2.3.0] - 2026-May-10

### Added

- **Delete local backups from within the app** — each entry in the Local Backup list now has a delete button (trash icon). Tapping it shows a confirmation dialog then permanently removes the file from all locations (Downloads, internal ADB copy) in a single operation.
- **BYD Atto 2 Boost** — 51.1 kWh Blade LFP (100S estimated), FWD, 130 kW, 344 km WLTP.
- **BYD M6 Standard 120kW** — 55.4 kWh Blade LFP (102S), DC charging 89 kW.
- **BYD M6 Superior 150kW** — 71.8 kWh Blade LFP (132S), DC charging 115 kW. The existing M6 Superior entry has been renamed to "BYD M6 Superior 100kW" to distinguish the two; its stored ID is unchanged so existing car selections are preserved.
- **BYD Seal 6 Premium 95kW** — 56.64 kWh Blade LFP (118S, 150Ah × 377.6V), FWD, DC charging 103 kW, 425 km WLTC.
- **BYD Seal 6 Premium 160kW** — same 56.64 kWh pack (118S), RWD, 160 kW rear motor.
- **Distance shows no decimals in split-screen mode** — the Distance power metric no longer displays a digit after the decimal (e.g., "12.3") when the app resides in split-screen. Integer format is used to avoid crowding.
- **Added AUD (Australian Dollar) currency option** — the electricity cost tariff now supports A$ in addition to €, £, and $.

### Changed

- **Battery temperature removed from Battery stat card** — the value is unreliable across firmwares. Cars without a valid battery temp source (e.g. Seal Excellence) were displaying firmware sentinel values (186÷3=62°C, 195÷3=65°C, etc.) that could not be reliably distinguished from real readings without a stable ambient reference. The data continues to be recorded and displayed in the charging detail chart where it is more trustworthy.
- **rawJson no longer duplicates dedicated column data** — 25 fields already persisted in typed DB columns (soc, speed, gear, latitude, longitude, altitude, tyre pressures, tyre temperatures, cell voltages, battery total voltage, 12V voltage, soh, engine speeds, electric range, soc panel) have been removed from the per-point JSON blob. New data points are ~700 bytes smaller per row (~33% reduction). Fields that have no dedicated column (drive\_mode, regen\_mode, car\_on, statistic\_\*, instrument temps, bodywork diagnostics, etc.) are retained.

### Fixed

- **Battery cell temperature from statistic device auto-detects the encoding** — the BYD SDK returns `cellTAvg` / `cellTMin` / `cellTMax` in one of two encodings depending on firmware: direct °C on some firmwares, 1/3 °C units on others. Applying the wrong interpretation produced either an underread (e.g., 11.5°C when ambient was 32°C) or an overread (e.g., 62°C when ambient was 25°C). The fix uses the in-car ambient temperature as a reference and picks the interpretation whose deviation from ambient is physically plausible (within ~30°C parked/driving, ~50°C during DC fast charging).
- **Battery temperature sentinels suppressed when ambient is unavailable** — `resolveStatisticTempCelsius` was returning the divided value (raw ÷ 3) even when no ambient reference was available to validate it. Firmware sentinels like 186 (→ 186÷3=62°C) passed through undetected if the instrument device had not yet reported outdoor temperature. The function now returns null until ambient is known.
- **Service self-stop tripwire no longer fires on fresh start** — `lastFeatureEventElapsedMs` defaults to 0, which previously made the "SDK silent for 10 minutes" check evaluate to true immediately on first launch (any uptime exceeded the threshold before any SDK events arrived). The service would self-stop before SDK devices could register, blocking all telemetry until the user physically drove the car. The tripwire now requires at least one real SDK event before the silence threshold is evaluated.
- **Off-state idle no longer thrashes the service overnight** — three independent periodic restart sources (`ServiceWatchdogWorker` 15-min, `ServiceRestarterJobService` periodic 15-min, and `ServiceRestartReceiver` boot-action kicks) were re-starting the telemetry service throughout the night, undoing the carOff+notCharging self-stop and keeping the wake lock + WiFi lock held for ~33% of the overnight period. A new persistent `ServiceIdleState` flag is set when the service self-stops; every periodic restart source now reads the flag and skips the restart while idle. `BootReceiver` no longer re-arms restart kicks on `ACC_OFF` / `POWER_DISCONNECTED`. The off-state wake-lock duty cycle drops from ~33% to ~5%, eliminating the overnight 12V drain.
- **Service idle self-stop now truly stops the service** — the previous implementation called `stopSelf()` but did not set `intentionalStop = true`, causing `onDestroy` to immediately reschedule a restart via `scheduleSelfRestart`. The service was effectively cycling: stop → restart → 5 min → stop → restart, running all night and continuously publishing MQTT snapshots. Setting `intentionalStop = true` before `stopSelf()` breaks the loop; the service stays stopped until the next 90-minute alarm fires intentionally.
- **MQTT idle publish cadence corrected** — as a consequence of the above fix, MQTT now publishes a single snapshot every ~90 minutes while the car is off and idle (from the `OffStateKeepaliveReceiver` alarm), rather than every 5 minutes from the unintended restart loop. The MQTT settings hint has been updated to reflect this: "Idle: snapshot every ~90 min".
- **BYD SDK temperature sentinel values unconditionally rejected** — `resolveStatisticTempCelsius` now rejects raw values of 195 (`TEMPERATURE_INVALID`) and -60 (`TEMPERATURE_MIN`) before any ambient comparison logic. Previously these sentinel values could pass through when no ambient reference was yet available, producing spurious readings such as 65°C (195 ÷ 3) on the Seal Excellence firmware.
- **Live trip tracking card consumption matches finalized trip stats** — the energy counter in the trip tracking card now uses the battery discharge counter delta (`currentTotalDischarge − sessionStartTotalDischarge`), the same source used by finalized trip statistics. The previous motor power integration approach excluded AC, 12V auxiliary, and other non-motor loads, causing a systematic under-read of 10–20% when climate control was active.
- **Live trip tracking card average speed matches finalized trip stats** — average speed is now derived from the pure odometer delta (`currentOdometer − sessionStartOdometer`), matching the `endOdometer − startOdometer` formula used by finalized trips. The integration-based distance estimate is retained as a fallback only when the odometer has not yet advanced.
- **Backup deletion for "Download (file)" entries now works on Android 10+** — backups created by older app versions that wrote directly to the filesystem had no MediaStore record, so `File.delete()` and MediaStore-by-name lookups both silently failed. The deletion logic now follows a 4-step escalation: direct `File.delete()` → MediaStore lookup by file path (`DATA` column) → MediaStore lookup by display name → insert the file into MediaStore to gain ownership, then delete via the returned content URI.

### Maintenance

- **User-triggered "Trim database" action** in the Local Backup screen (new "Maintenance" section). Replaces the earlier auto-compaction-on-startup approach so the user runs it at their convenience and gets explicit feedback. Five phases run sequentially with live progress:
  - **A. Strip redundant rawJson fields** from every row in `trip_data_points` and `charging_data_points` (the 25 keys already persisted as typed columns).
  - **B. Clear rawJson** to `{}` for trip and charging points older than **45 days** — the diagnostic payload is no longer needed for historical data and recovers the largest amount of space.
  - **C. Downsample old trip points to 1 row per minute** for trips older than **60 days**. The native sampling rate of ~1 row per 30 s is preserved for recent trips; older trips keep `MIN(id)` per `(tripId, minute)` bucket. Distance, energy, and time-series shape remain accurate; chart smoothness is slightly reduced for very old trips.
  - **D. Delete charging data points for AC sessions older than 45 days.** AC vs DC is inferred from `charging_sessions.peakKw < 25 kW`; DC fast-charging sessions are kept entirely (rare and interesting to review). The session summary row (kWh added, peak kW, duration, etc.) is preserved for both.
  - **E. VACUUM via raw SQLite** — the telemetry service is briefly stopped, Room is closed, and `PRAGMA wal_checkpoint(TRUNCATE)` + `VACUUM` are run on a direct SQLite connection (the database file would otherwise stay locked by Room's connection pool, leaving freed pages unreclaimed). The app then auto-restarts so Room reopens cleanly — same pattern used after a database restore.
  - The result and timestamp are persisted across app restarts; the section header always shows "Last trimmed: …" with a one-line summary of the rows affected. Logged with tag `DatabaseTrimmer`. Expected to recover the bulk of the 500 MB+ accumulation since v2.1.0.

---

## [2.2.0] - 2026-May-08

### Added

- **Imperial / Metric unit system** — users can now choose between Metric (km, km/h, kWh/100km) and Imperial (mi, mph, kWh/100mi) in Settings → Preferences. The default is determined by the device locale (UK → Imperial, all others → Metric) and can be changed at any time with a two-button selector; the active choice is highlighted in the primary colour. The preference persists across restarts.
  - Odometer and speed values are telemetry-sourced and already arrive in the correct unit for the vehicle's market (BYD UK cars report miles and mph natively), so no conversion is applied to those — only the unit label changes.
  - Calculated distances (trip distance, monthly totals, range projection) and efficiencies (kWh/100km → kWh/100mi) are converted at display time; stored data always remains in metric.
  - The unit system is respected across the Dashboard, Trip History, Trip Detail, Trip Compare, Trip Goals, Seasonal Analysis, and all charts (Speed, Instant Consumption, Consumption history, Range Projection).
- **Live trip stats in the Trip Tracking card** — while a trip is being recorded, the card shows a real-time centre column: elapsed time (HH:MM:SS, continuous — does not pause during brief car-off stops within the 30-minute resume window), average speed, total energy used (kWh), and average consumption (kWh/100km or kWh/100mi). The card stays at a fixed height; the stats appear between the gear/status section on the left and the Auto toggle/Stop button on the right. All values respect the active unit system.
- **SoC overlay on the 12V / HV battery history chart** — the 48-hour battery monitoring dialog now overlays the HV State of Charge as a dashed purple line on a dedicated right-hand axis (0–100 %), making it easy to correlate 12V drain events with HV top-up cycles. SoC is sampled and persisted alongside the existing voltage readings. The dialog title was updated to "HV / 12V Batteries - Last 48 Hours" to reflect that both battery systems are now shown.
- **BYD Tang EV support** — the pure-electric Tang (AWD, 108.8 kWh Blade LFP, 530 km WLTP) is now available as a selectable BEV profile alongside the existing Tang DM-i.
- **In-app changelog viewer** — tapping "Changelog / What's new" in Settings → About now opens a scrollable in-app dialog instead of launching the browser.

### Changed

- **`enginePower` type corrected to `Int`** — the BMS reports motor power as a whole-number kW value; the model field was previously widened to `Double` unnecessarily. Decimal precision is only meaningful for `chargingPower`, which originates from a separate telemetry path and remains `Double`. All redundant `.toInt()` / `.toDouble()` conversions and the now-unreachable `.isFinite()` guard have been removed.
- **Off-state keepalive redesigned** — the redundant in-service 3-minute MCU wake loop has been removed; only the alarm-based `OffStateKeepaliveReceiver` remains. Its interval has been increased from 4 minutes to 90 minutes, allowing the MCU to genuinely sleep between pokes (~75 min idle per cycle) and significantly reducing overnight 12V / HV standby drain. `POWER_CONNECTED` remains the primary wake-up trigger for charging detection, so a charge session started by plugging in is detected immediately; the 90-minute alarm is a fallback backstop only. During active off-state charging sessions, WiFi is kept alive via a targeted keepalive every 10 minutes so that charging telemetry is not interrupted when the car is off.
- **Service self-stops when idle** — after the car has been off and not charging for 5 minutes, the telemetry service stops itself and releases the CPU wake lock, allowing the infotainment to enter deep sleep. This eliminates the overnight HV→12V DC-DC cycling that was observed as a steady SoC drain while parked. The 90-minute alarm restarts the service briefly for a periodic snapshot, then the self-stop fires again. Charging sessions are unaffected: the service stays alive for the full duration of any active charge.
- **Idle poll interval extended to 5 minutes** — when the car is off and not charging, the telemetry loop now ticks every 5 minutes instead of every 30 seconds, reducing unnecessary CPU wake-ups and repository calls. The AC/slow-charging case retains 30-second polling for SoC granularity. The MQTT settings hint has been updated to reflect the new publish cadence: driving at the configured interval, charging every 30 s, idle: service sleeps (no publish).
- **SCREEN_OFF keepalive removed** — the dynamically registered `SCREEN_OFF` receiver that called `McuWakeHelper.keepAlive()` on every screen-off event has been removed. It was firing at the exact moment the system tried to sleep, resetting the MCU WiFi countdown and preventing deep sleep. WiFi continuity during charging is now handled solely by the targeted 10-minute keepalive in the telemetry loop.
- **12V history chart Y-axis fixed** — the voltage axis is now always 12.0–14.0 V (previously 11.0–14.0 V). The SoC right-hand axis ticks were updated from five labels (0/25/50/75/100 %) to three (33/66/100 %) to match the three voltage gridlines.

### Fixed

- **BYD M6 cell count** — Now the number of cells is correct, which means SoH will be calculated correctly
- **12V chart crosshair showing `%.2f` literally** — the 12V voltage value in the crosshair tooltip was not being formatted; it now correctly displays the numeric value (e.g. `13.70 V`).

---

## [2.1.1] - 2026-May-06

### Added

- **BYD ATTO 2 support** — new BEV trims added to the car catalog: Active and Comfort.
- **BYD M6 support** — the M6 is now available as a selectable BEV profile.
- **Public compatibility probe export** — the compatibility probe can now be saved as `Download/BydTripStats/compat_probe.json` locally instead of only being generated in the app cache and sent via private telegram bot.

### Changed

- **SoH handling** — SoH estimates are now capped at 100% before they reach the dashboard, MQTT, and history due to `bodyworkBatteryCapacity` being rough or slightly inflated number and `remainingKwh / soc` could overshoot when one of the inputs would be coarse or stale
- **Probe export flow** — the Settings screen now only reports success after the public Downloads file has actually been written.
- **Regen mode detection** — additional firmware paths are now used to surface drive/regen modes on more car models beyond the original supported set.
- **Vehicle analysis naming** — the trip analysis screen now uses the broader `vehicleAnalysis` label instead of `phevAnalysis`.
- **AWD icon rendering** — the drivetrain icon in the dashboard was switched to a sharper bitmap path, improving clarity on the car head unit.

### Fixed

- **Compatibility probe save** — the saved probe file now uses the same Downloads path as the app’s other backup/export flows, so it actually appears in the file manager on supported BYD head units.

### Reverted changes

- **Road slope×** — the value is no longer divided by 10 at the read (change implemented at 2.1.0)

---

## [2.1.0] - 2026-May-01

### Added

- **BYD Seagull / Dolphin Surf** — three new BEV trims added to the car catalog: Active, Boost and Comfort.
- **BYD Seal 5 / Sealion 5 DM-i** — two new PHEV trims added to the car catalog: Comfort and Design.
- **48-hour live 12V history chart** — tapping the dashboard `HV / 12V` stat card now opens a rolling 48-hour chart of the auxiliary battery voltage, recorded from live telemetry in a dedicated history buffer so users can inspect 12V health and overnight drain behaviour independently of trip or charging-session history.

### Changed

- **Battery stat card** — title is now always "Battery"; value shows "SoH: X.X%" with one decimal place; subtitle shows the average battery temperature ("Temp: X.X °C") when available.
- **PHEV false-positive detection** — sentinel values (≥ 100,000) returned by BEV firmware for fuel-system fields are now excluded from the PHEV signal count; `getEnergyMode` is also excluded for confirmed BEV models, preventing BEV cars from being mis-classified as PHEVs during the compatibility probe sweep.
- **Motor RPM detailed chart** — the fullscreen/detailed view now uses peak-only bucketing at 480-point resolution (4× the condensed thumbnail) so the front motor shows a natural flat-zero baseline with real engagement spikes, rather than an artificially dense spike pattern. The condensed dashboard thumbnail is unchanged.
- **AWD motor stat card** — subtitle now shows total drive power only (e.g. "AWD · 187 kW") instead of a fabricated per-axle split that assumed a fixed front/rear ratio regardless of driving conditions.
- **Duration format** — minutes are now shown as "min" instead of "m" across trip history, trip detail, charging history, charging detail, and trip compare to avoid ambiguity with meters. Hours format updated accordingly (e.g. "1h 19min").
- **Drive/regen mode timeline** — each lane (Drive mode, Regen mode) is now individually gated on its own data availability. Previously, if one mode had data but the other did not, the mode-less lane rendered as a solid grey bar. Now each lane is only shown when it has at least one recorded data point with a known mode value.
- **Background poll rate** — when the car is off, the telemetry poll interval is reduced from 1 s to 30 s, significantly reducing CPU active time and 12V battery drain during overnight parking and long AC charging sessions. An intermediate 5 s tier applies when the SDK has sent events within the last 60 s (e.g. a remotely-awoken car sitting in Park), keeping mode changes visible promptly without continuous fast polling. DC fast charging (above 23 kW) retains 1 s polling for accurate power curve resolution. The service remains always-on as a foreground service; no data or functionality is lost. The delay now precedes the poll work and is based on the previous tick's state, eliminating any unnecessary fast poll on a car-on to car-off transition and maximising idle sleep time.
- **Regen / Driving Dynamics stat card** — renamed from "Regen / Driving Mode"; road grade (slope in degrees) is now shown in the subtitle when available, relocated from the Environment card where it did not fit contextually.
- **Drive/regen mode prompt** — the "Regen / Driving Dynamics" stat card on the dashboard now shows a hint when the car is on but either mode has not yet been detected, prompting the user to tap a mode on the car display. Trip Detail also shows a persistent info chip above the mode timeline when a saved trip is missing drive or regen mode data, with a message explaining how to enable recording on future trips.

### Fixed

- **Drivetrain mode label (AWD / FWD / RWD) always blank** — on some firmware builds the drivetrain state getter returns 0 when polled outside an active event callback. The app now falls back to the car catalog drivetrain type when the getter returns 0, so the motor stat card shows the correct AWD / FWD / RWD label throughout the drive.
- **Road slope reading 10× too steep** — the raw SDK value is in tenths of a degree but was being treated as whole degrees, so a barely noticeable descent would display as e.g. −6° instead of −0.6°. The value is now correctly divided by 10 at the read site and displayed with one decimal place.
- **Range projection chart changes after navigating away and back** — when returning to the dashboard from another screen (or after the app process was recreated), the range projection chart could gain an orange "Projected (actual)" line that was not visible during the live drive session before navigation. This happened because the trip restore path always assigned a projected value to every data point, including those below the calibration threshold. The restore path now leaves unstabilised points without a projected value, matching live session behaviour.

---

## [2.0.0] - 2026-Apr-28

### Added

- **Standalone telemetry runtime** — the app now reads vehicle data directly from the BYD DiLink SDK without requiring Electro or an MQTT broker. All dashboard, trip recording, and charging session features run from the built-in runtime path.
- **Physics-based energy breakdown** — Trip Detail Analysis tab shows how trip energy splits across rolling resistance, aerodynamic drag, elevation gradient (climb / descent), and auxiliary losses (12 V system, HVAC, residual). The model uses Crr = 0.0074 (calibrated against real trips, consistent with ISO 28580 for LFP EV tyres), CdA and kerb mass from the car catalog, and drivetrain efficiency η = 0.88. Components are scaled proportionally so they always sum to the measured consumed energy.
- **ABRP integration** — live telemetry can be forwarded to A Better Route Planner via the Link Generic API using a user-provided token. Includes a test-upload action and token visibility toggle. Opt-in only — disabled by default.
- **Outbound MQTT integration** — live telemetry can be published to any external MQTT broker (e.g. HiveMQ or HomeAssistant) using an Electro-compatible JSON schema, a configurable interval, and a test-publish action. Opt-in only — disabled by default.
- **Connections tab** — dedicated Settings screen for managing ABRP and MQTT integrations, each with a summary card and last-sync timestamp.
- **Drive and regen mode analytics** — drive mode (Normal / Eco / Sport) and regen mode are recorded with every trip data point, shown as a colour-coded timeline chart in Trip Detail, and summarised in the Analysis tab.
- **Combined Power + SoC charging chart** — Charging Detail now includes a dual-axis view with an X-axis toggle between elapsed time and SoC percentage, making DC taper shape easy to read.
- **Per-trip custom DC charging cost** — Trip Detail can override the home-tariff cost estimate with the actual amount paid at a public charger.
- **Environmentals card** — the dashboard temperature card now shows ambient temperature alongside PM2.5 indoor/outdoor readings where the car exposes them.
- **App Diagnostics monitor** — a toggleable section in Settings → Data showing live CPU, RAM, thread count, and uptime with 60-second history charts and an integrated ADB shell runner for diagnostics.
- **Pre-update database backup** — a local database snapshot is created automatically before any APK update is installed.
- **Autostart management shortcut** — the post-update autostart reminder can deep-link directly into BYD’s native Disable Autostart screen where the firmware exposes it.
- **CarCatalog physics fields** — all catalog entries now include `estimatedKerbMassKg`, `cdA`, and an `isPhev` flag. PHEV entries also carry `phevUsableBatteryKwh` for correct EV-range projection.
- **PHEV range projection** — for PHEV models the range projection combines estimated EV range (from usable battery capacity) with the BMS fuel range, giving a combined EV+ICE projection. The chart ceiling adapts to the combined range rather than the EV-only WLTP figure.
- **VehicleCompatibilityProbe** — runtime probe that records which SDK getter methods are available on the connected car, used for diagnostics and future compatibility reporting.

### Changed

- **Telemetry pipeline** — dashboard, trip recording, charging sessions, and all stat cards are now fed by the standalone runtime path. Electro/HiveMQ is no longer involved in normal operation.
- **Charging data quality** — charging power, SoC, HV voltage, and cell min/max now come from verified live in-car signals rather than heuristic reconstruction. Peak and average power in Charging Detail derive from real recorded samples.
- **Battery temperature** — shown as the midpoint of cell min/max when no confirmed pack-average source is available, rather than a potentially wrong single-sensor value.
- **Odometer** — uses the verified decimal mileage source where available, eliminating the `.0` truncation visible on some firmware builds.
- **Distance card** — during a trip that continues across a short engine-off break, the dashboard shows both the current engine-on segment distance and the cumulative trip distance separately.
- **Automatic stop behaviour** — manually stopping an auto-detected trip now warns the user and switches tracking back to manual mode until re-enabled, preventing accidental re-arm.
- **Consumption charts** — outlier buckets outside 9–35 kWh/100 km are filtered; legends show the live average; the selected-duration average is plotted alongside the car reference line.
- **Consumption thumbnail** — the dashboard chart thumbnail previews the monthly consumption line instead of the noisier daily view.
- **Trip timeline sampling** — long trips are sampled evenly across the full duration instead of showing only the earliest events.
- **Tariff editor** — currency symbol entry uses a styled dropdown of common symbols instead of free-text input.
- **Tyre dashboard** — temperature is now shown and tyre unit label text enlarged for readability on the DiLink display.
- **Dashboard animation control** — motion-heavy effects (liquid battery, energy flow canvas) can be disabled from Preferences. Disabling animations reduces CPU usage by up to 80% on DiLink firmware.
- **Foreground notification** — shows a counting-up elapsed timer ("1:23") anchored to service start instead of a wall-clock timestamp that reset to "0m" on every update. Status text now shows car name, SoC, and charging power only — gear, speed, and motor power removed.
- **Range projection on late Activity open** — if the app is opened after a trip has already started, the range chart now reconstructs historical points from the database and shows both the BMS and projection lines from trip start, not from when the app was opened.
- **Range projection stability on return from background** — switching to CarPlay or home and returning no longer resets the chart. `currentTripId` is now kept alive in the ViewModel scope regardless of Activity visibility, preventing the race where a transiently null trip ID caused the chart to restart from the current position.
- **Update install flow** — silent PackageInstaller sessions are used where available so granted runtime permissions survive the update.
- **Release build** — R8 minification and resource shrinking enabled; `dontwarn` rules added for optional HiveMQ/Netty classes.

### Fixed

- **Charging icon / `isCharging` detection** — charging-active state now follows real charging evidence rather than stale fallbacks, eliminating false pulse animations.
- **Charging session flapping** — brief telemetry gaps no longer generate spurious `0 m` junk sessions.
- **Trip auto-start after process recreation** — dashboard state and service binding now survive DiLink activity churn more reliably.
- **Battery voltage and charging-power mapping** — HV pack voltage and charging-power signal paths corrected against verified car telemetry.
- **Trip detail chart layout** — overlapping Y-axis labels and overly dense instantaneous-consumption ticks reduced.
- **Settings back navigation** — returning from nested settings pages now restores the correct top-level tab.
- **Trip metrics on active trips** — max speed, max power, and max regen update on every telemetry packet, not only when a throttled data point is written to the database.
- **Engine-off trip continuation** — trips correctly continue across engine-off breaks shorter than the configured window without ending prematurely on the return to P.

### Removed

- **Electro/MQTT runtime dependency** — the app no longer requires an Electro topic, an embedded broker, or any MQTT connection for normal operation.
- **Legacy bridge code** — the embedded MQTT broker, the Electro-era external MQTT client, and all compatibility-only broker/topic parameters have been removed.
- **Obsolete Electro-era UI copy** — legacy MQTT-facing prompts and stale Electro-only wording removed throughout.

---

## [1.4.2] - 2026-Mar-29

### Added

- **Seal U** — support is added for Seal U Design and Seal U Comfort.

### Fixed

- **Live Charging Session Visibility** — resolved an issue in the Charging History screen where active/live charging sessions were filtered out and hidden; active sessions are now correctly displayed and pinned to the top of the list.
- **Range projection spikes** — during sustained regen or heavy braking the projected range would spike to 500+ km then crash back down. Root cause: negative energy values (`enginePower < 0`) were being subtracted from the rolling consumption window, making the denominator collapse toward zero. Regen is already captured in the SoC-based numerator — the fix excludes negative samples from the rolling window so only forward driving energy contributes to the Wh/km estimate.
- **App returns to "no trip" state after using CarPlay** — when DiLink killed and recreated the Activity while the user was in CarPlay, the new Activity instance bound to `MqttService` asynchronously and late, leaving a window where `observeMqttServiceState()` had not been called and the UI showed disconnected / not in trip. Fixed by moving bind to `onStart` and unbind to `onStop` (standard Android lifecycle pattern). The foreground service, trip recording, and MQTT connection were unaffected throughout.
- **"IVI system does not support this operation" popup on update** — `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` permission triggered a system dialog that DiLink's IVI OS rejects with this error. Removed both the manifest permission declaration and the `requestBatteryOptimizationExemption()` call. The existing `PARTIAL_WAKE_LOCK` in `MqttService` provides equivalent keep-alive behaviour without requiring a system dialog.

### Changed

- **FAQ** — added note clarifying that trips recorded in areas without signal (e.g. underground garages) may take up to 3 minutes to appear in history. The watchdog closes the active trip after 3 minutes of telemetry silence, anchored to the last received packet before signal was lost.

---

## [1.4.1] - 2026-Mar-24

### Added

- **Background Trip State Restoration** — the `DashboardViewModel` now reconstructs its live graph and efficiency counters from the database upon app resume, preventing data loss when the system kills the process in the background.

### Fixed

- **Unsupported Battery Optimization** — resolved issues with background service termination on certain DiLink firmware versions.

---

## [1.4.0] - 2026-Mar-23

### Added

- **Immediate Power-On Restart** — plugging in a charger now broadcasts `ACTION_POWER_CONNECTED`, which immediately revives the MQTT stack via the `BootReceiver` rather than waiting for the 15-minute watchdog.
- **Autostart Reminder** — after each app update (which resets DiLink's autostart permission), the app now detects the version change and displays a one-time reminder dialog asking the user to re-enable Autostart in car settings. An explicit warning also appears on the initial setup screen.
- **Battery Degradation Screen** — tap the Battery Health stat card on the dashboard to open a dedicated SoH-over-time view. Plots average SoH per trip, a least-squares trend line, a dashed projection line, and a "Projected @ 80%" year label. Four summary cards: current SoH, lowest recorded, decline rate (%/yr), and projected 80% year. Health interpretation legend with BYD Seal warranty note (70% / 8yr / 250 000 km). X-axis labels shown only on first point and on any point where SoH drops, keeping the axis uncluttered.
- **Seasonal Consumption Analysis** — new screen accessible from Trip History toolbar (☀️ icon). Groups all completed trips by meteorological season and shows avg kWh/100km per season as a colour-coded bar chart (🌱☀️🍂❄️) with reference line from `CarConfig`. Each season card shows total distance, kWh consumed, avg battery temp, and delta vs reference. Auto-generated winter-penalty insight when both winter and summer data are present.
- **Trip Goals & Personal Bests** — new screen accessible from Trip History toolbar (🏆 icon). Personal bests: lowest ever efficiency, longest single trip, longest consecutive daily driving streak. Goals: set a consumption target (avg of last 5 trips) and/or a monthly distance target; animated progress bars update in real time. Goals persist via `SharedPreferences`.
- **Cost Tracking** — tap the € icon in the Trip History toolbar to set your electricity tariff (price/kWh + currency symbol). Trip cost is displayed inline in the energy consumed chip (`4.40 kWh (€0.62)`). A collapsible monthly cost summary card shows up to 12 months of history. Trip Detail also shows cost as a row in the stats card.
- **Hybrid Charging Session Recording** — two complementary mechanisms now cover all charging scenarios:
  - *Car ON*: real-time session opened when `chargingPower > 0`, data points recorded every second, session closed immediately when charging stops or car turns off. Live Power / SoC / Voltage / Temperature charts available in Charging Detail.
  - *Car OFF*: SoC + timestamp persisted to `SharedPreferences` on every telemetry packet. On next car wake-up, if SoC increased by ≥ 1% (≥ 0.3 kWh), a synthetic session is created with accurate kWh from the delta. Covers overnight charging, timed charging, and remote charging with zero background process requirement.
- **Multi-model compatibility** — `VehicleTelemetry` fields absent on FWD-only models (Atto 3, Dolphin) now carry safe defaults so `kotlinx.serialization` never throws `MissingFieldException` on a missing key. The motor stat card adapts its title and kW subtitle to `Drivetrain.FWD` / `RWD` / `AWD`.
- **14 Heatmaps** — four new heatmaps added: Tyre Pressure vs Consumption, SoC vs Regen Efficiency, Speed vs Battery Temperature, Cell Voltage Spread vs SoC. The Cell Voltage Spread chart uses a widened y-tick column (70 dp) to prevent label overlap with the rotated axis label.

### Changed

- **Charging History UI** — summary card replaces "Peak ever kW" with "Avg SoC gain %". Synthetic (car-off) session cards show a "⚡ Reconstructed" badge and omit peak/avg kW chips. Real-time (car-on) sessions display an active badge and support live Power / SoC / Voltage / Temperature charts in the detail view.
- **Charging Detail charts** — tabs now show a contextual empty state for synthetic sessions explaining that live charts are only available for car-on sessions, rather than a generic "Not enough data" message.
- **MQTT — persistent session** — `MqttClientManager` now uses a fixed client ID (`"BydTripStats"`), `cleanSession(false)`, and QoS 1 subscription. Combined with "Retain published values" in Electro, this ensures the broker delivers the latest state immediately on reconnect rather than waiting for the next publish interval.
- **Service watchdog** — removed `NetworkType.CONNECTED` constraint so the watchdog fires every 15 minutes regardless of network availability.
- **`carConfig` load order** — `MqttService` now loads car config synchronously before starting the MQTT connection, eliminating a race condition where the first charging packet could arrive before `carConfig` was set.

---

## [1.3.0] - 2026-Mar-20

### Added

- **Charging Session Management** — implemented full support for deleting single and multiple charging sessions from the Charging History screen.
- **Bulk Deletion** — long-press a completed session to enter selection mode, then tap the trash icon to delete multiple sessions at once.
- **Electro Documentation** — added recommended MQTT intervals (1s Car On, 30s Car Off) to the Initialization screen, Network tab, and FAQ.
- **Service Watchdog** — a periodic WorkManager job (every 15 minutes) checks that the embedded MQTT broker and MQTT client are alive and restarts them if the OS had killed the process. Ensures charging telemetry is received even when the car is off and the head unit is under memory pressure.
- **Charging Chart Styling** — charging session charts (Power, SoC, Voltage, Temperature) now match the trip detail aesthetic: `primaryContainer` card background, styled borders, and an interactive crosshair showing precise value, clock time, and elapsed charge duration on drag.

### Changed

- **Atomic Transactions** — `ChargingRepository` and `TripRepository` now use `database.withTransaction` to ensure that mass deletions of sessions and data points are fully atomic and performant.
- **Car-Off Charging Reliability** — removed the 10s car-off watcher shutdown. The MQTT background service now remains active during charging even when the car is turned off, ensuring continuous charging curve capture.
- **Debounce Optimization** — standardized the charging detection debounce to 60 seconds (1 minute), allowing a generous margin for the new 30s recommended Electro car-off interval.

---

## [1.2.2] - 2026-Mar-19

### Added

- **Auto-Update Mechanism** — the app now automatically checks for newer versions and silently updates itself when the vehicle is parked and no tracking is active.
- **Video Documentation** — added a YouTube presentation link and thumbnail to the README for onboarding new users.

### Fixed

- **Charging Session Tracking** — resolved an edge case where AC/DC charging sessions would fail to finalize and continue tracking indefinitely.

---

## [1.2.1] - 2026-Mar-18

### Fixed

- **Post-shutdown CPU spike** — MQTT service now stops automatically after 10 s of `car_on = 0`, preventing the reconnection storm against a dead broker that caused ~71% CPU usage in the minutes following car shutdown. The watcher is guarded by `!isInTrip` so it never interrupts an active trip.
- **Background animation overhead** — `LiquidFillBattery` wave, glow, and bolt-pulse infinite transitions now snap to static values when the lifecycle state is below `RESUMED`. `EnergyFlowCanvas` flow animation applies the same gate. Both were previously cycling at 60 fps regardless of whether the screen was visible.
- **Compose recomposition rate** — added `debounce(500L)` to the `combine(isInTrip, currentTelemetry)` collector, capping dashboard redraws at twice per second instead of once per every 1 s MQTT packet.
- **Unused parameters removed** from `TripRepository.thinOldDataPoints()` — `olderThanMonths` and `keepEverySeconds` were dead code after the tiered thinning policy was introduced in 1.2.0.

---

## [1.2.0] - 2026-Mar-17

### Added

- **Charging session tracking** — automatic detection of AC and DC charging sessions via `chargingPower` field. Each session records SoC start/end, kWh added, peak and average kW, battery temperature rise, and HV voltage. Detection uses a time-based debounce (60 s for DC ≥ 20 kW, 12 min for AC) anchored to `lastChargingTelemetry` so `endTime` reflects actual charge completion rather than debounce fire time.
- **Charging history screen** — dedicated screen (battery icon on dashboard) listing all sessions with duration, SoC arc, kWh added, and peak kW. Active sessions shown with a live badge.
- **Charging detail screen** — four-tab detail view per session: Overview (summary metrics), Power (charge curve — DC taper shape), SoC (fill over time), Voltage (HV bus rise), Temperature (avg + cell min/max).
- **Trip comparison** — select 2–3 completed trips in Trip History (long-press → checkboxes) and tap the compare icon. Opens a bottom sheet with:
  - **Summary tab** — side-by-side metric table with winner highlighting per row.
  - **Charts tab** — overlaid Speed, Power, Consumption, SoC, and Elevation lines normalised to 0–100% trip distance so trips of different lengths compare cleanly.
  - **Routes tab** — all routes drawn on a shared OSM map in distinct colors. Per-trip eye toggle hides/shows individual trips across all three tabs.
- **Battery voltage chart** (trip detail) — dual-axis: HV bus (left, V) and cell min/max band (right, V). Cell spread highlighted in crosshair — key early indicator of cell imbalance.
- **Tyre pressure chart** (trip detail) — all four wheels over time, with inline bar/PSI/kPa unit switcher that shares state with the dashboard tyre icon preference. Dynamic Y-axis step and padding adapt per unit.
- **Instantaneous consumption chart** (trip detail) — raw `power/speed×100` kWh/100 km with 5-point rolling average overlay. Filters to speed > 5 km/h; zero line marks the regen/drive boundary.
- **Panel SoC line in SoC chart** — `soc_panel` rendered as a second line (violet) alongside BMS SoC (blue) with legend. Only shown when the trip has non-zero panel values (v2+ telemetry).
- **Electro telemetry v2 fields** — `VehicleTelemetry` now maps: tyre temperatures (×4), `soc_panel`, `car_locked`, `any_door_opened`, `fuel_percentage`, `fuel_driving_range_km`. Tyre temps and `soc_panel` promoted to first-class `TripDataPointEntity` columns.
- **Database migration 1 → 2** — adds 5 columns to `trip_data_points`, creates `charging_sessions` and `charging_data_points` tables with proper foreign keys and indices.

### Changed

- **`toRawJson()` writes only non-default values** — empty driving points now store `{}` (2 bytes) instead of the static 38-byte string, reducing the `rawJson` column by ~94% on BEV trips. PHEV fields gated by `isPhev` flag.
- **Database maintenance worker** — schedule changed from monthly to weekly. Checkpoint changed from `FULL` to `TRUNCATE` (actually reclaims WAL disk space). Thinning policy replaced with a four-tier age-based policy: < 7 days untouched, 7–30 days → 1 point/2 s, 30–90 days → 1 point/10 s, > 90 days → 1 point/30 s (edit on 1.4.0: This changes to 1 point /15 s instead).
- **Range projection** now uses `selectedCarConfig.batteryKwh` instead of the hardcoded Seal Excellence constant, making projections accurate for Dolphin and Atto 3 owners.

### Fixed

- `MotorRpmChart` `LegendDot` visibility conflict with `OsmRouteMap` — each chart file now carries its own `private` definition.
- `TripCompareSheet` routes tab map touch handling — replaced `pointerInteropFilter` (which consumed events before MapView received them) with `setOnTouchListener` + `requestDisallowInterceptTouchEvent`, allowing correct pan and pinch-to-zoom. OSM built-in zoom buttons removed (`Visibility.NEVER`) to free the bottom legend area.
- `TripCompareSheet` routes tab height overflow during sheet drag animation — `Box(Modifier.weight(1f).clipToBounds())` applied at two levels prevents the map from escaping the sheet boundary.

---

## [1.1.0] - 2025-Mar-13

### Added

- **Initialization screen** — first-run setup screen that prompts the user to select their BYD model and enter their Electro MQTT topic before the app becomes usable. Settings are persisted atomically so neither value can be missing after setup.
- **Car catalog (`CarConfig`)** — structured per-car configuration covering drivetrain, battery capacity, WLTP range, reference consumption (kWh/100 km, sourced from ev-database.org), and recommended front/rear tyre pressures. Supported models at launch:
  - BYD Seal Dynamic (RWD, 61.4 kWh)
  - BYD Seal Premium (RWD, 82.5 kWh)
  - BYD Seal Excellence (AWD, 82.5 kWh)
  - BYD Dolphin Active / Boost (FWD, 44.9 kWh)
  - BYD Dolphin Extended / Comfort / Design (FWD, 60.4 kWh)
  - BYD ATTO 3 (FWD, 60.4 kWh)
- **In-app car switcher** — tapping the car name in the dashboard top bar opens a dialog to change the selected model without going through settings.

### Changed

- **Motor RPM card (dashboard stats)** is now drivetrain-aware:
  - FWD cars show "Front Motor — N RPM" only.
  - RWD cars show "Rear Motor — N RPM" only.
  - AWD (Seal Excellence) shows both axles with a proportional live kW estimate (front 160 kW / rear 230 kW split, confirmed per BYD factory specs; total 390 kW combined).
- **Motor RPM chart (trip detail)** shows only the motor(s) present on the selected car. The legend and crosshair tooltip adapt accordingly (FWD: front only; RWD: rear only; AWD: both).
- **Heatmaps tab (trip detail)** — motor-related heatmaps adapt to drivetrain:
  - "Motor RPM vs Speed" title and axis label reflect the driven axle(s).
  - "Front vs Rear Motor RPM" torque-split heatmap is only shown for AWD cars.
- **Tyre pressure indicators** now use the recommended pressures from the selected car's config (previously hardcoded to Seal Excellence values). Alarm thresholds (±0.2 bar) remain car-agnostic.
- **Consumption chart reference line** uses the selected car's reference consumption from the catalog instead of a hardcoded constant.
- **Range projection chart** caps the projected range at the selected car's WLTP figure instead of a hardcoded 520 km.
- **MQTT connection status** in the Settings Network tab is now split into three distinct visual states with matching icons:
  - `SyncProblem` (red) — connection error with error message.
  - `Sync` (green) — connected and actively receiving telemetry.
  - `SyncDisabled` (muted) — disconnected or no data yet.
  - Same three states are mirrored in the dashboard top bar icon.

### Fixed

- `PreferencesManager.getMqttSettings()` would hang indefinitely due to using `collect` (infinite terminal) instead of `first()` on the DataStore Flow.
- First-run race condition: saving the car selection and the MQTT topic as two sequential DataStore writes could cause the topic write to be skipped if recomposition navigated away from the initialization screen between the two writes. Both are now written in a single atomic `edit` block via `saveInitialSetup()`.

---

## [1.0.0] - 2025-Mar-08

### Added

- Initial release.
- Live MQTT telemetry dashboard (speed, power, SoC, range, gear, tyre pressures, battery stats, motor RPM).
- Automatic and manual trip recording with gear-based auto-detection (D/R → start, P → stop).
- Trip history with per-trip charts: speed, power, SoC, altitude, energy consumption, motor RPM, route map.
- Trip detail heatmaps: power vs speed, consumption vs speed, regen vs speed, RPM vs speed, battery temp vs power, acceleration vs speed, SOC vs consumption, time-of-day vs speed, gradient vs consumption.
- Range projection chart with BMS estimate reference line and power-integrated realistic projection.
- Weekly / monthly / yearly consumption charts with car average reference line.
- Liquid-fill battery indicator with charging animation.
- Energy flow canvas with animated directional arrows (acceleration / regen / charging states).
- Tyre pressure indicators with per-axle alarm thresholds and BAR / PSI / kPa unit switcher.
- Local backup to `Download/BydTripStats/` (SQLite).
- Telegram bot backup with manual send and scheduled auto-backup (daily / weekly / monthly).
- CSV and JSON trip export.
- Embedded MQTT broker on port 1883 for local Electro integration.
- Boot receiver and foreground service to keep MQTT alive without manual app restarts.
- DatabaseMaintenanceWorker for automatic old-data pruning.
- Dark-themed Material 3 UI optimised for the BYD DiLink 11 in-car infotainment display.
