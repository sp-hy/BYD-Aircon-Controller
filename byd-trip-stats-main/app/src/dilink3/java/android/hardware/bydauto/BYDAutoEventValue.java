package android.hardware.bydauto;

// Compile-only stub. Real class (concrete) provided at runtime. Only the field this app currently
// reads (intValue, for the generic onDataEventChanged feature-ID dispatch) is declared.
public class BYDAutoEventValue {
    public int intValue;
}
